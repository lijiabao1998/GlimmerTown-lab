# 微光小鎮·實驗線 — 給 Claude 的常駐規則

業主 2026-09-24 口述，對本倉庫的每一個 Claude session 生效。施工流程本身看 `AUTORUN.md`，本檔只補常駐規則和雲端對照。

## 常駐規則

1. **預設就是實驗線。** 業主沒點名時，說的一律是本倉庫（`lijiabao1998/GlimmerTown-lab`）。主線 `lijiabao1998/GlimmerTown` 只讀，業主明說「改主線」才動。
2. **串台要及時提醒。** 實驗線是 T420 從主線分出來的，檔案、文件、程式碼裡有大量主線殘留：卡號、版本號、四方治理、車位、`verify.py`、`C:\dev\glimmer-town` 路徑等。主線的規矩不會自動適用這裡。拿不準某條規則、某個數字、某個卡號屬於哪條線時，先跟業主講，不要自己套用。
   - 已知撞號：**T578–T589 兩條線都有，內容不同**（例：實驗線 T588＝個體跟隨原型，主線 T588＝城市健康閉環）。業主說卡號時預設是實驗線；落在撞號段就先確認。
3. **美術第一。** 實驗線的優先序是美術 ＞ 其他一切（市民一生、效能、工具）。其他事只在美術需要它、或業主點名時做。（`AUTORUN.md` 第 5 節已同步改序。）
4. **全局觀感的改動先給業主看圖。** 色調、光影、密度這類改了就影響整張畫面的，先臨時做兩三檔、拍同一畫面的對照圖讓業主挑，不替業主選，拍完還原。T619 夜色就是這樣定的：Claude 推薦最深的 C，業主看圖選 B，因為 C 會逼出一批為了夜景硬加的燈。

## 雲端 session 對照（`AUTORUN.md` 寫的是業主 Windows 本機）

- `AUTORUN.md` 第 1 節的 `新的迭代\` ＝ 本倉庫根目錄；Windows 路徑在雲端不存在，邊界等價於「只寫本倉庫」。
- 推送：綠燈 commit 後推 `origin main`（只准快進，禁 `--force`），session 另有指定分支就一併推。
- **2026-09-25 起多寫入者**：遠端 `origin/main` 是唯一真相源，雲端、本地誰寫都可以。開工前 fetch、先推卡面佔號、推送前再 fetch；遠端前進了就把自己沒推的 commit rebase 上去、重跑煙霧再推。細則見 `AUTORUN.md` 第 2 節「遠端」開頭的框。
- 業主的決定記在 `docs/DECISIONS.md`，開工前讀一次。
- **寫入者分工（2026-09-25）**：`main` 只給 Claude、Codex 推；Grok（`grok/*`）、GLM（`glm/*`）、Kimi（`kimi/*`，業主之後可能升 main）走分支開 PR；GPT（ChatGPT 聊天版）不寫倉庫，只讀、審、出草稿——業主轉來的 GPT 草稿或補丁當別人的草稿處理，重新探查、寫卡、跑守衛與煙霧才進倉庫，卡上註明來源。規則寫在根目錄 `AGENTS.md`（給不讀本檔的工具看，內容一致）。Claude 是 `main` 寫入者之一，看到分支的 PR 要照 `AGENTS.md` 第 3 節合併：本地 squash、重跑煙霧 3 連綠與 `fp.js --check`、配 T 號、寫日誌、推 main、在 PR 留言後關閉；要業主看圖的先放上決策單。
