# T604 收完三件：72px 圖集、4×2 分戶、工業拆到 2×2

**基線**：`68750c0` T603  
**業主**：全部做完。

## 三件

1. **72px 圖集對齊詞典**  
   `buildSprites` 之後 `stampAtlas604()` 把 k=1/2/3、lv1–3、v0–11（含住宅財富鍵）蓋成 1×1 街區精靈的縮圖。左右牆、尖頂／平頂／棚跟城裡同一本 `ARCHE568`。`__noAtlas604` 可關（須在 boot 前設）。
2. **4×2 樓房改一戶一戶**  
   進深 ≥2 的住宅不再整棟交給英美立面（強制 `__facadeForce577` 時仍交）。平頂也畫分戶牆和門。`__noBay604` 退回整棟立面。
3. **工業 4×4 拆開**  
   工業視覺合併上限 2×2。`__noIndSplit604` 退回 4×4。

## 不改

存檔、模擬、主線、8123。

## 驗收

- finish604 10 項：圖集 modern≠cottage、shed≠workshop、72px 左右都有像素；4×2 不走 f577；工業 4×4 收到 ≤2。
- 煙霧綠。
- 超街區快取 `997a4336 → 47016a0f`（4×2 住宅分戶）。72px 圖集指紋台這輪報 0（後置蓋章與指紋腳本抓到的是同一份 SPR，但 bld 葉 CRC 未進差異表——以自測 `__t604` 與互異 CRC 為準）。

## 施工紀錄

- 煙霧綠 19.4s；finish604 10/10。第一次分戶把 ukStyle 的強制立面也跳過，改成有 `__facadeForce577` 仍走立面。
- after：`shots/T604_city_after.png`。
- **沒做成**：① 72px 蓋章是縮圖，不是重畫 72 像素手稿 ② 財富鍵與中產同一剪影，只是不再用舊 isoBox ③ 工業 2×2 內部仍是一塊廠，不是一戶一戶
