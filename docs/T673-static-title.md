# T673 — 分頁標題寫死的舊版號

**輪次**：r120
**基線**：`d1f6593`（v13.76／T671）
**狀態**：已落地（v13.77）；卡面先單獨提交 `e06fe32`
**來源**：本 session 的現況審查（缺漏審查代理）：`index.html` 第 10 行 `<title>微光小鎮 Glimmerville · v13.16 T583</title>` 停在 T583；遊戲載入後才由程式改寫成「城名 · v版本」（`index.html` 約 68426、68552 行）。分享連結、書籤、Pages 首頁在程式跑起來之前看到的都是 v13.16。

---

## 1. 這一輪要解決什麼

靜態標題上的版本號從 T583 起就沒人改（每次升版改的是 `GAME_VER`、`GAME_ANCHOR`、開始畫面 `startVersion456` 三處）。實測 Pages（`https://lijiabao1998.github.io/GlimmerTown-lab/`）的原始 HTML 標題也是 v13.16。

## 2. 改什麼／不改什麼

**改**：靜態標題拿掉版本號，改成 `微光小鎮 Glimmerville`。版本號照舊由程式在載入後補上，所以以後升版不用多改一處、也不會再過期。

**不改**：程式改寫標題的兩處；`GAME_VER`、`GAME_ANCHOR`、開始畫面版本字的升版流程；任何畫面。

## 3. 怎麼算做完（動手前寫下）

1. 原始 HTML 的 `<title>` 不含 `v13.16`、不含任何 `v數字`（`grep`）。
2. 煙霧 3 連跑綠；進城後 `document.title` 仍是「城名 · v13.77」這種有版號的樣子（煙霧後另跑一次讀 `document.title`）。
3. `fp.js --check` 全綠、零變動。

## 4. 施工

- `index.html` 第 10 行 `<title>` 改成 `微光小鎮 Glimmerville`（加註 T673）。版本 v13.77／T673。沒有新閥門：只是一行靜態文字，舊行為（寫死舊版號）不值得保留成開關。

## 5. 施工紀錄（照實記）

| 項目 | 結果 |
|---|---|
| 驗收 1 | 原始 HTML `<title>` 含 `v數字` 的：0 處 |
| 煙霧 | 3/3 綠（28.9、28.3、27.2 秒） |
| `fp.js --check` | 全綠、零變動（只有已登記的 4 葉環境差） |
| 標題實測 | 主選單：`微光小鎮 v13.77`（程式改寫，有版號）；進城後：`晨曦里 · 微光小鎮`（沒有版號） |

**卡面第 3 節第 2 條寫錯了**：動手前以為進城後標題是「城名 · v版本」，實測進城後是 `index.html` 約 51645 行改成「城名 · 微光小鎮」，本來就不帶版號（改之前也一樣，這張沒動它）。帶版號的只有主選單那一段。第 2 條改成實測到的兩個值，照實記在這裡。

**沒做的**：
- 進城後的分頁標題不帶版號，是既有行為，沒改。
- Pages 上線後的原始 HTML 標題要等這次推送、Pages 重建後才會變；推送前實測線上還是 `v13.16 T583`。
