# T582 — 街廓風格凝聚（英式區／美式區）

**輪次**：r33　**基線**：T581 v13.14

## 1. 探針結論
- `arche568(k,lv,v)` 的消費端在 `makeBlockSprite547` 內部（無 x,y 可用）
- `pickV406(k,lv,x,y,fallback)` **有 x,y** 且決定 v ⇒ v 決定原型 ⇒ 原型的 `fs` 決定風格
- 方案：在 `pickV406` 裡加**區域風格偏置**——用 `hashLocal479(x>>3,y>>3,salt)` 決定
  該 8×8 街廓偏好的風格家族（UK/US），在 `rank` 序列中偏向對應的 v 值範圍

## 2. 驗收條件
1. 同一 8×8 街廓內的 RCI 建築，其 `fs` 欄位 80% 以上屬於同一家族（UK 或 US）
2. 不同街廓可以有不同家族（多樣性）
3. 不消耗 R()、不動模擬、不進存檔
4. 逃生閥 `window.__noStyleDistrict582`

## 3. 施工
（完成後填）

## 4. 守衛
（完成後填）

## 3. 施工
`pickV406` 加區域風格偏置：`hashLocal479(x>>3,y>>3,56901)` 決定 8×8 街廓的風格偏好
（cohort<0.33 → bias −0.18 偏 UK 原型、cohort>0.67 → bias +0.18 偏 US 原型、中間不偏）。
偏置加到散佈值 q 上，使同街廓的建築 v 值集中，從而偏好同一風格家族的原型。

## 4. 守衛
| 項目 | 結果 |
|---|---|
| 指紋 | 0/0/0（凝聚改的是 v→原型映射，不改 sprite 本身） |
| 煙霧 | **綠 11.8s** |
| 版本 | 13.16／T583 |

## 5. 未盡
1. 凝聚效果需要**有人的城市**才能看到（無頭新建城市 pop=0 無建築）。
2. 偏置強度（±0.18）是初步值，可能需要調校。
3. `hashLocal479` 的街廓粒度（8 格）可能太粗或太細，需實測。
