# T615 文化休閒十二類重畫：博物館、劇院、水族館、電影院、動物園、遊樂園、會展中心、植物園、體育園區、購物中心、幼兒園、農貿市場

**基線**：`9bf3f15`（T614 v13.24）
**來源**：`variants574/cul_a～f.js` 由另一帳號的 Claude 設計（檔頭自標「T614 文化休閒第三刀」），2026-09-24 14:28 因週額度中斷。這條線的 T614 已經用在市政類，所以本卡改號 T615。**原作與接手的部分分開記**：凍結版 `d5204ce` 對本卡最終版的 diff 只落在 cul_e 與 cul_f，內容全部列在「接手修改」。

## 場景

文化休閒類是城市的地標層，還停在舊畫法。原作已經把十二類裡的十類畫到可以收；剩下的兩類，一類只畫了一個變體，另一類根本沒畫：

- **k87 農貿市場**（cul_f）：`const VV=[V0,V0,V0]`，v1、v2 是 v0 的佔位複本，三張圖逐像素相同。
- **k65 大型購物中心**（cul_e）：檔頭寫了，程式裡沒有 `build(65,…)`。遊戲裡還是舊的**正立面**圖：扁平正面、底下一個橢圓影子、車子正面排成一列，完全不是等距視角；三個變體只差一座塔或一座摩天輪。

## 改什麼

- 六檔嵌進 `<!-- T610 batches -->` 區塊。
- **接手修改**（都在草稿檔內，原作的部分不動）：
  1. cul_f k87 補畫 **v1 紅磚市場廳**：屋脊沿 v 方向、山牆正面掛 MARKET 招牌、屋脊採光氣樓、+u 側拱窗與卸貨月台、前廣場攤位、兩側停車與卸貨。
  2. cul_f k87 補畫 **v2 露天農夫市集**：三排小攤與陽傘攤、產地直送小貨車、木造服務亭、燈串。
  3. cul_e k65 **整類新畫**，沿用 cul_e 自己的 LIB：
     - v0：郊區 L 形購物中心（兩層主館、SHOP／STORE 兩棟錨點店、玻璃中庭配金字塔天窗、大停車場、路邊招牌塔）；
     - v1：城市垂直商場（裙樓加退台塔樓、LED 大螢幕、空中花園、三層開放式立體停車場）；
     - v2：露天購物街（U 形兩層街屋、各店的牆色、遮陽篷、招牌都不同，轉角鐘樓，中庭噴泉）。
  4. cul_e 共用工具 `signR` 修正：在 +u 面上，v 越大越靠畫面左，字卻從 va 端（畫面右端）往右寫，整串字會寫到面外。改成從高 v 端起寫、置中。這個錯誤原作的 k56 也有：一處單字母小招牌偏出右緣 1–2 px。
  5. cul_e 的迭代開關 `DBG=[]` 改成 `null`。開著時每個圖層都要多做一次 `getImageData`。
- 新增自檢 `cultureSelftest615`，版本 13.25／T615。

## 不改什麼

- 原作其餘十類的內容一字不動。
- 草稿檔頭自標的卡號保留原樣，讓原作可以追溯：civ_* 自標「T613」、落地為 T614；cul_* 自標「T614」、落地為 T615。對照關係只記在卡面，不回頭改檔頭。
- 不動模擬、存檔、佔地（MSZ）、繪製端；只有 `bld` 家族會變。

## 驗收（動手前寫）

1. `cultureSelftest615` 全綠：
   - 十二類三十六張都通過 T612 共用自檢；
   - 都換成了新圖（沿用 T614 的 `wave614_pre` 快照）；
   - 六個批次都跑了、零例外；
   - 版本錨 ≥T615。
2. 煙霧測試綠；console 零錯誤。
3. 指紋 `--expect=bld`，棘輪 OK。
4. `perf.js --check` 在門檻內；記下開機增量。
5. 接觸表與城裡實景逐類看過。「疊在一起」逐張檢查：業主反覆抱怨的就是這種問題。

## 審查補輪（接手時做的）

- `probe-wave614.js`：十二類全部通過。其中 k65 在第一版探針下是**假綠**（見 T614 卡），加上「真的換掉了」這條之後才抓得到。
- 人眼：六檔接觸表逐張看過。
  - 原作沒有審查輪的 cul_a（博物館、劇院）、cul_c（動物園、遊樂園）、cul_e 的 k56 體育園區、cul_f 的 k84 幼兒園都可以收。
  - 中斷時正在第三次重試的 cul_d k47 v2（現代植物館）：檔案裡就是第三版，畫面完整，照收。
- 補畫與新畫的部分自己踩到、也已經修掉的問題：
  - k87 v2 第一版三排間距不夠，中排陽傘壓在後排篷頂上（「疊在一起」），拉開排距、錯開位置。
  - k87 v1 門口白傘看起來像一團白塊，也擋住大門：改綠色，移到旁邊。
  - k65 v2 轉角店的兩個立面全被左右兩排擋住，招牌只冒出半截「KET」：壓低轉角店，拿掉看不到的立面，只留鐘樓。
  - k65 v2 咖啡傘壓在遮陽篷上：退到中庭內。
  - k65 v0／v2 的 `signR` 字飄在面外：修工具本身（上面第 4 點）。

## 施工紀錄

- **整合**：六檔照 T610 格式嵌入。cul_b、cul_c 兩檔是整檔 CRLF（凍結版 `d5204ce` 本來就是這樣），嵌入時轉成 LF，跟前例 trans_a、logi_a 的處理一致；草稿檔本身保持原樣。整合腳本對孤立 CR 仍然會擋。`index.html` 7.82 MB、52 個 script 區塊語法全過、CR＝0。
- **煙霧測試綠**（18.9s）：`culture615` 11 項全過，包括「十二類都換成新圖」「六批都跑了零例外」「cul_e DBG 已關」；`civic614` 等其餘自檢仍綠；console 零錯誤。
- **指紋** `--expect=bld`：新增 0、變更 36（12 類×3）、移除 0，只觸及 `bld`，其餘 150 族零變動。超街區快取一致，棘輪 OK。`fp.json`、`style.json` 已重錄為 13.25／T615。
- **效能** `perf.js --check`：全在 ±25% 門檻內，最差的是 rot1 −7.8%。
- **開機成本**：六個文化批次這次量到合計約 **1.4 秒**。最重的是 cul_e 473 ms（k56 用光線步進畫曲面，加上 k65 三張 272×280 大畫布），其次 cul_c、cul_d 各 355 ms。整條佇列 60 批 7.8 秒、零例外。**量測雜訊很大**：同一個 logi_a 在兩次量測間從 782 變成 671 ms、trans_a 從 673 變成 547 ms，所以單次數字只能當量級看。開機優化仍然掛在 T618。
- **城裡實景**：`shots614/T615_city_s1～s3_*.png`（不進版本庫）。k65 的 v1、v2 與 k56 都坐在地塊上；1×1、2×2 類比例正常。
- **沒做成**：
  ① 開機又多了約 1.4 秒，這一卡沒有優化。
  ② k65 v0 郊區商場的停車場占了半塊地，在城裡縮小看時車子是一片雜色，沒做遠景簡化。
  ③ 補畫與新畫的部分（k87 v1／v2、k65 全類）只有我自己審，沒有第二雙眼睛；原作的部分也一樣，同模型不算獨立覆核。
