# T593 — 屋頂設備密度隨 lv＋招牌新舊掛 age

**輪次**：r42　**基線**：T592（d19270c）　**狀態**：完工

## 施工

1. **屋頂設備密度隨 lv**（sprite 側）：makeBlockSprite547 兩處 roofKit559 的
   area 參數乘 `(1+(lv-1)*.35)`——lv3 屋頂設備比 lv1 多 ~70%。sprite 鍵含 lv
   ⇒ 決定性；fp --expect=block559 宣告變動（棘輪 OK，151 族零誤傷）。
2. **招牌新舊差異**（draw-side drawSignAge593）：商業（k=2）age≥12 在店面帶
   疊一塊隨年齡褪色的招牌板——新屋飽和色＋邊緣高光、老屋褪色＋剝落缺角＋
   下方垂漬 1~2 條；五組 hue 由 streetHash 決定性分配。`__noSign593` 閥、
   `__t593SignCount` 計數。

## 附帶修正（守衛過程抓到）

- **detailPermit432 槽位表缺 591/593** ⇒ 兩層落 .16 乘層幾乎全抑制——補映射
  `591:1,593:1`（與 426 同槽＝牆面敘事族）。
- **drawSignAge593 座標翻倍**：bx0 誤含 bx 再被 R() 加一次 ⇒ 招牌畫出畫布外
  （計數器 21 但畫面零差異的怪象即此）——改相對座標。
- **perf 台帽正規化**：無頭 rAF 無 vsync，快場景破百 fps 而慢場景貼 60 帽，
  跨帽讓比值假摔（本輪 night/rot3「-40%/-60%」實為絕對 fps 提升）——比值改用
  min(fps,60) 計算並重錄基線後全綠（±5%）。

## 驗收

1. 招牌層同 run A/B：diff=true、cnt=21 命中。**PASS**。
2. fp --expect=block559（屋頂密度）＋fp --check=0（draw-side 零誤傷）。**PASS**。
3. perf --check 綠（帽正規化＋重錄基線，比值差 ≤5%）。**PASS**。
4. 煙霧測試綠（17.5s、0 error）。**PASS**。
