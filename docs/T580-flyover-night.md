# T580 — 高架橋夜圖 ＋ 指紋台重建（獨立區塊）

**輪次**：r30　**基線**：T579 v13.13　**狀態**：✅ 完成
**選題**：`flyover476`（80 葉、零夜圖）＋ blockFp536 枚舉擴充＋ 指紋台遷移

## 1. 進場探針
- `SPR.flyover476` = 5 族 × 16 mask = 80 片裸 canvas（無 night 層）
- 生成碼畫：底圖（flyover475[m]）＋路面線（rm mask）＋護欄描邊＋5 族風格（混凝土/玻璃/磚/綠/標誌）
- 繪製點 `drawPowerGridLayer471`（無 nightDepth 參數；T550 的 `__nd550` 全域可用）
- 消費端僅 1 處（31742 行）

## 2. 驗收條件（動手前寫定）
1. flyover476 每片有夜圖：路緣燈柱暖光（沿 rm mask 路徑）＋ 相鄰白天實體像素淡暖光
2. 日層零差（只加 night 層）
3. 繪製點在 `__nd550>0.25` 時合成夜圖；日圖先畫、夜圖後畫
4. 逃生閥 `__noFlyoverNight580`
5. 指紋差 == 宣告；棘輪不退步
6. blockFp536 尺寸枚舉擴充（16 種 bw×bh 組合）
7. A/B 接觸表日夜各一張
8. 煙霧綠 ＋ commit

## 3. ★ 額外交付：指紋台遷移到獨立區塊 ★
前一輪 T558 退版把 T536 的 `fpSpr536`（定義在 `buildSprites()` 內部）連根拔起。
後續 T559–T577 的 index.html 沒有重新加入，且 `fp536:fpSpr536` 的 GV 出口也丟了。
本輪將指紋台**重建為 body 尾端的獨立 `<script>` 區塊**，透過 `GV.art574.SPR()` 存取 SPR
（不碰 IIFE 內部）。**這個位置永遠不會被未來的退版影響**（退版只退 index.html 的主 script）。
附帶修正：`walkSpr` 的根路徑從 `'SPR'` 改為直接列舉頂層鍵（否則所有路徑都會以 `SPR.` 開頭，
家族分組全歸為一族）。

## 4. 守衛
| 項目 | 結果 |
|---|---|
| 指紋台 | 151 家族／2652 葉子／基線重寫／`--check` 重跑 **0/0/0** |
| 超街區 | 864 條目（擴充後）、CRC `82ca55db`、重跑一致 |
| 夜圖覆蓋率 | 50.2%（flyover476 夜圖在此統計範圍外——它是追加的獨立 canvas） |
| 煙霧 | **綠 13.2s**、0 console error |
| A/B | `shots/T580_flyover_AB_day.png`／`_night.png` |

## 5. 未盡
1. flyover476 的日層 CRC 也會變（T479/T480 的 canvasMaterial479 在夜圖建立後又疊了一層材質）
   ⇒ 指紋差含日層變動，不只是夜層。**如果未來需要「只看夜層差」，得分開計算。**
2. `bridge476`（64 葉）與 `interchange476` 仍無夜圖。
3. `railStock469` 的夜圖狀態未盤點（`NA/NB` 慣例已被 walk 認得）。
