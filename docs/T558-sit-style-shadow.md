# T558 — 落地＋風格混用＋別墅細密＋貼地影

**輪次**：r27　**結果**：❌ **煙霧紅燈 → 已退版**（見文末）
**基線**：`6215e26` T557 v13.10
**業主**：有建築懸空；角度和風格要多（現代／古典）；陰影要更好；別墅要有很多細節。繼續。

## 驗收（動手前）

1. 自己看 after：樓的牆腳必須碰到地坪／草地，不能漂。貼地影不依賴 quality=2。
2. 同一座城同時有：聯排、別墅（花園＋坡頂＋門廊）、現代平頂、古典石牆。不是全城陶瓦帳篷。
3. 2×2 以下住宅走別墅（綠籬、小徑、獨立屋頂、煙囪），不是整塊填滿佔地。
4. 煙霧綠。8199 不關。

## 不改

存檔、人口、就業、6×7、分條遮擋、夜燈鐵律。

## 逃生閥

`__noStyle558`：回到 T557 三型。`__noShadow558`：關掉新貼地影。

---

## ❌ 收束：煙霧紅燈，依鐵律退版（2026-09-14）

**狀態**：`git checkout -- index.html` 退回上一個綠點 `6215e26`（T557 v13.10）。
退回後煙霧 **綠 8.6s**、T558 痕跡殘留 0（`__noStyle558`／`styleOf558` 皆 0 次）。

### 紅燈原文

```
X 紅燈（8.3s）
   - UI 點擊往返執行失敗: evaluate 例外: TypeError: Cannot read properties of null (reading '2628')
   - 事件流往返執行失敗: evaluate 例外: TypeError: Cannot read properties of null (reading '2628')
```

### 已知的事實

- `2628` ＝ `idx(36,36)`（地圖中心格）——T534 的 UI 往返與 T540 的事件流往返**都在地圖中心找空地**，
  兩支同時掛掉，指向 `tiles` 取用失敗（`T(idx(x,y))` 對 null 取索引）。
- 前面幾階段（主選單／烘焙／主迴圈／功能自我測試）**全過**，只有會碰 `tiles` 的兩支掛掉。
- 查過一個可疑點：新增的 `const N=[sxOf(x,y)+32*z, syOf(x,y)]`（33200 行）區域變數 `N`
  **遮蔽了模組層的地圖邊長 `N`**。但讀了包圍函式 `drawLotShadow558()` 全文，
  該函式只是把 N 當「北角點」用，**沒有把 N 當地圖邊長** ⇒ **這個命名是壞味道，但不是紅燈主因。**

### ★ 沒查出來的（照實寫）★

**我沒有定位到真正的根因。** 線索只到「會碰 `tiles` 的測試掛掉」為止。
要定位就得把 T558 的 99 行改動逐段二分（例如先只留 `__noStyle558` 分支、再只留貼地影），
**我沒有做，直接依鐵律退版。** 這是取捨：鐵律要求「不准帶著紅跑下一輪」，
而單次比對的成本高於重做一次的預期成本。

### ★ 另一個必須講清楚的損失 ★

**退版把整個 T558 的 99 行實作一起丟掉了。** 因為它們**從未 `git add`**，
`git checkout --` 之後無法從 reflog 或物件庫找回。**這張卡是目前唯一的痕跡。**
⇒ **流程修正建議：長輪次施工中，每到一個小段落就先 `git add`（甚至 `git stash`），
讓「退回上一個綠點」不必等於「整輪重做」。**

### 下一步（換一件事做）

業主在卡首列的四項需求（**建築懸空／風格要多／陰影更好／別墅細節**）**一項都沒交付**。
建議下次**拆散重做**：先只做「貼地影」一件（`drawLotShadow558` 當時是自帶逃生閥的獨立函式，
爆炸半徑最小），綠了再往上疊風格混用，最後才動別墅細節。
**一次只做一件，紅燈時才知道是哪一件。**
