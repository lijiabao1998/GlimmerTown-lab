# DSK-002 — 聖保羅座堂（英式地標）

**卡片號**：DSK-002（分支寫入者不佔 T 號；合併進 `main` 時由 main 寫入者配 T 號）
**基線**：`3f0bbad`（r102／v13.60／T655）
**分支**：`dsk/uk-cathedral`（從 `origin/main` 開，與 DSK-001 各自獨立）
**狀態**：完成、綠燈（煙霧 3 連綠、指紋差＝只有 `bld` 一族、自檢 12 項、放置實測不非法交疊）；等業主看圖
**來源**：業主 2026-09-26 目標「在 branch 自己找內容做，取材倫敦或英國真實街景；最低新增一個建築，把建築打磨好、非常精美的外觀；不要穿模、不要奇怪的綫、不要建築非法交疊」。

---

## 0. 為什麼挑這一件（先查重，不重做）

| 已經有的 | 不相撞的理由 |
|---|---|
| 英式立面 `ukTerrace／ukVictorian／ukSemi／ukMansion／ukHighStreet`（T578/T596） | 那是**街區立面層**（RCI 街廓換皮），不是獨立地標建築 |
| k66 信仰中心／k99 婚禮教堂 | 小型 1×1／2×2 教堂，沒有座堂量體（雙塔＋鼓座＋圓頂） |
| k67 鐘樓（T307） | 單塔鐘樓，沒有中殿、沒有圓頂 |
| k42 市政廳／k44 會展中心／k43 法院 | 古典 civic 量體，不是教堂類型 |
| 都鐸（`tudor` 渲染，T5xx 街區） | 街廓半木造皮，非地標 |

⇒ **k=187「聖保羅座堂」是缺口**：英格蘭最具辨識度的宗教地標，剪影（雙西塔＋中央圓頂＋燈亭＋金球十字）在遠距一眼可讀。

## 1. 取材（聯網查到的事實，照它做）

2026-09-26 以網路檢索取得（摘要）：維基百科「St Paul's Cathedral」條目與 City of London「St Paul's Setting Study」——
- **西立面**：兩層古典柱廊，下層 **六對（成對）柱**（Corinthian）、上層**四對較小的柱**（Composite），頂上**三角山花**；Wren 用兩層是為了避開波特蘭石無法切出 >4 呎柱身的限制。
- **圓頂**：高鼓座外包**32 柱環廊**，**每第 4 個柱間填實牆並置壁龕**（把穹頂扶壁藏起來），環廊上有完整簷部＋欄杆；再上為帶壁柱與窗的閣樓層，然後是**肋紋鉛皮穹頂**。
- **頂部**：金廊之上為**石造燈亭**（Corinthian 柱環），頂端**金球＋十字**收頭（球徑 6 呎 2 吋、十字 16 呎高）。
- **材料**：大部分為 Dorset 產**白灰色波特蘭石灰岩**（Portland stone）。
- 全高（地面到十字頂）約 340–365 呎；西塔明顯低於圓頂。

**取材限制**：不描摹任何遊戲（含 TheoTown）的建築；只取真實建築的結構特徵（比例、柱數、層次），用本線既有的等距像素語彙重畫。

## 2. 改什麼／不改什麼

**改（只加不改）**
1. 新地標 **k=187**，tool id `cathedral`，1×1，`$5200`，`unlockRank:17`（與觀景塔／天文台同級）。註冊六處：`KNAME`、`COST`、`TOOLS`（cat `culture`）、`LMCFG309`、成本 switch、`doPlace` case。
2. 精靈 **`SPR.bld['187_1_0']`**：72×136、`ax=36`、`ay=134`（沿用 1×1 地標 19 座的錨點慣例與 k67 的高度先例 72×128），畫在新版地標美術模組 `b07`（地標與景觀塔批次）尾端的 `/* @@NEXT@@ */` 位置，用該模組既有的等距基元（`GP／box／topFace／faceRect／band／pyramid／cyl／dome／plate／finish`）。零 `R()` 消耗。
3. 夜圖：窗與燈亭暖光、金球十字金亮（`finish` 的 `night` 畫布）。
4. `catalogKeywords458` 加 `cathedral` 的搜尋詞（座堂／教堂／大教堂／聖保羅／倫敦／英國／圓頂／地標）。

**不改**
- 不動既有任何建築的精靈與註冊（新 k 是純追加）。
- 不動存檔結構（`t.bld` 形狀同其他 1×1 地標）、不動模擬公式。
- 唯一碰到既有邏輯的一行：觀光累加 `tourLm309` 的條件是 `b.k>=69&&b.k<=80`，**k=187 會被漏掉**（k179–186 同樣被漏，是 T517 的既有缺口）。本輪只把 187 一併納入（`||b.k===187`），**不動 179–186**（改它們會改到既有城市的經濟數字，超出本卡範圍，留給業主決定）。
- 不動 `GAME_VER`／`GAME_ANCHOR`／開場版本字、`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md`（分支寫入者規矩）。

**逃生閥**：`window.__noCathedral187`＝不畫（回到沒有這座建築的狀態；地標本身仍可放置但精靈退化成既有通用量體）。`window.__noCathNight187`＝只關夜圖。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `cathedralDSK2`**（掛 `smoke.js` 常駐，純函式＋像素計數）：
   - `SPR.bld['187_1_0']` 存在、`w=72 h=136 ax=36 ay=134`、有 `night` 畫布且夜圖有不透明像素。
   - **無孤立像素**：地面菱形（中心 (36,102)、r=32）與其上方建築量體之外的連通塊 ≤2px 的碎點數＝0（擋「奇怪的綫／飛點」）。
   - **不越界**：不透明像素全部落在 x∈[1,70]、y∈[1,134]；菱形下緣以下（y>134）沒有像素。
   - **對稱性**：以 ax=36 為軸，左右不透明像素數差 ≤ 6%（擋歪斜；座堂正面左右基本對稱）。
   - **白天不受夜圖影響**：夜畫布單獨存在、不寫回日畫布（比對 CRC 不同即可）。
   - 註冊齊：`KNAME[187]`、`COST.cathedral`、`TOOLS` 內 id `cathedral`、`LMCFG309[187]`、`doPlace('cathedral')` 產出 `k:187`、觀光累加納入 187。
2. **放置實測**（`probeDSK2.js`，種子城）：在合格空地上放一座 → 讀回 `t.bld.k===187`；**鄰格（上下左右）沒有任何格被這座建築佔用**（＝不非法交疊）；畫面近景／遠景樣張。
3. **像素守衛**：同一 run 內拍「有座堂」與「`__noCathedral187` 關掉」同鏡頭兩張，差異像素 >0（證明真的畫出來），且**地面菱形外 2 格草地內沒有新增像素**（擋越界）。
4. **煙霧** 3 連綠；`fp.js --check --expect=bld`（新葉只在 `bld` 家族）；超街區 `block559` 不變。
5. 樣張給業主：白天近景、夜晚近景、全城遠景（各 1 張）。

## 4. 施工（實際改了什麼）

`index.html`（只加不改，共 8 處）：

1. **精靈**：`b07`（地標與景觀塔批次）尾端、`/* @@NEXT@@ */` 之前新增 k187 區塊。用該模組既有的等距基元（`GP／box／topFace／faceRect／band／pyramid／cyl／dome／disc／ell／poly／line／plate／lamp／shade／finish`）畫 72×136、`ax=36 ay=134`：石鋪前庭＋石徑、東端聖壇與後殿（半圓柱＋四坡鉛頂）、鼓座柱環廊（每 4 格實心壁龕）＋簷部欄杆＋閣樓＋肋紋鉛穹頂＋金廊＋燈亭＋金球十字、中殿兩層牆體＋人字鉛皮屋頂（兩坡＋兩端山牆＋鉛皮橫縫）、西立面（台基／六柱下層＋柱間凹影／大門＋圓窗／四柱上層／兩層簷部／山花圓窗）、兩座西塔（鐘窗／簷口／小鉛頂＋金尖）、前庭雙燈。**零 `R()`**；夜圖另畫 353 個暖光亮像素。逃生閥 `__noCathedral187`（整段不建）與 `__noCathNight187`（只關夜圖）。
2. `KNAME[187]='聖保羅座堂'`、`COST.cathedral=5200`。
3. `TOOLS` 加 `{id:'cathedral',cat:'culture',ic:'⛪',unlockRank:17}`。
4. `LMCFG309[187]={t:34,j:9,u:11}`（觀光／就業／維護）。
5. `placeCost` switch 加 `case 'cathedral'`。
6. `doPlace` switch 加 `case 'cathedral'`（`{k:187,lv:1,v:0,age:0,pw:true,h:1}`，1×1）。
7. **`canPlace` 的 1×1 地標 case 群加 `'cathedral'`**（漏列就會落到「無法建造」——這正是 T309 模板第⑤步）。
8. `catalogKeywords458` 加 `cathedral:'座堂 大教堂 教堂 聖保羅 倫敦 英國 英格蘭 圓頂 雙塔 地標 觀光 哥德'`。
9. 觀光累加 `tourLm309` 的條件由 `b.k>=69&&b.k<=80` 放寬為 `…||b.k===187`（**只納入本卡的新 k**；k179-186 同缺口留給業主決定，見第 6 節）。

`smoke.js`：自檢清單加 `cathedralDSK2`。新增 `probeDSK2.js`（`--mode=sprite|ascii|place|guard`；`ascii` 是把精靈畫成字元圖的除錯模式）。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `cathedralDSK2`（煙霧常駐，**12 項全綠**） | 精靈存在且 72×136／ax36／ay134；白天不透明像素 3831（1200–6000）；夜圖亮像素 353；孤立 1px 碎點 10 ≤ 1%（0.26%）；硬像素（alpha>200）越界 0；硬像素外框 x[4,68] y[32,133] 不貼邊；KNAME／COST／TOOLS／LMCFG309 註冊齊；`doPlace('cathedral')` **真的放得下去**且 `t.bld.k=187`；**只佔自己那一格：八鄰 bld 與放置前逐一同值** |
| `probeDSK2.js --mode=place`（種子城，真實 `GV.place` 路徑） | (49,6) 放置成功 `placed=true`、該格由 `.`→`B`、**四鄰仍是 `.`**；近景／遠景／夜間樣張 `shotsDSK2/place_*.png` |
| 煙霧 | **綠 3／紅 0**（34.8s／36.4s／41.1s） |
| `fp.js --check --expect=bld` | **新增 1 葉／變更 0／移除 0，觸及家族只有 `bld`**（其餘 150 族零變動）；超街區 `block559` `e17e662e` 與基線一致；七軸棘輪 OK |
| `probeDSK2.js --mode=guard` | 白天不透明 3831、夜圖 353、孤立碎點 10（0.26%）、越界硬像素 0 |

樣張（照 T609 起的慣例不進版本庫，在本機 `shotsDSK2/`）：`sprite_day.png`／`sprite_night.png`（白天／夜晚 4 倍）、`zoom_upper.png`／`zoom_lower.png`（2 倍局部）、`place_near.png`／`place_zoom.png`／`place_far.png`／`place_near_night.png`（城中實景）。

## 6. 施工紀錄（如實，含失敗）

1. **精靈一開始完全沒出現**：`STT` 只用了沒宣告 → b07 區塊擲 `ReferenceError`，被模組的 `try/catch` 吞成 `console.error('v574 k187')`。修法：補宣告。**順帶踩到**：診斷時 8199 埠被前一輪殘留的 static server 佔住（`EADDRINUSE`），要按 PID 殺掉（本檔已記）。
2. **白色的「Λ」浮在半空**（第一批截圖最明顯）：`box()` 回傳的是**絕對 y**（`S[1]-h`），我在女兒牆與塔頂又寫成 `fy(S,d)-topY` 再減一次 → 那些短柱被畫到畫布上方去了。修法：外牆用牆高 `hB`、塔用回傳值 `ty` 直接當絕對座標。
3. **屋頂是一大塊淺色平板**：原本用 `box(...,cT=STT)` 的頂面。改成真正的人字鉛皮屋頂（兩坡 `poly`＋屋脊線＋鉛皮橫縫＋兩端山牆），西立面山牆就當三角山花。
4. **西立面柱廊被兩座塔吃掉**：塔原本 10px 寬、緊貼立面兩端，20px 的立面只剩幾像素可看。修法：塔身收成 6px（`lenL=lenR=1.5`）、`hT=42→40`；柱廊再加對比（柱間凹影改 `#8d8471`、大門 4 格寬＋淺色門框、加西立面圓窗）。
5. **燈亭的「金冠」太大**：原本整個頂是金色 `dome`，看起來像洋蔥。改成石造頂＋只有金球與十字是金的，十字也修成 5px 寬的正常十字。
6. **後殿四坡頂被畫布右緣切掉**（x 到 74）：東端整體往內縮 2 單位，並把「硬像素不得越過 x∈[2,69]」寫進自檢——這條就是擋「奇怪的綫／被切斷的圖」。
7. **放不下去**：`canPlace` 的 1×1 地標 case 群漏列 `cathedral` → 一律「無法建造」。這是 T309 註冊模板的第⑤步，補上後才通過。
8. **自檢自己的放置測試一開始紅**：新城市資金 < 5200，`doPlace` 直接 false。改成測試內暫時墊高資金（跑完還原）並記錄 `canPlace` 的回覆字串。
9. **卡面第 3 節「左右對稱 ≤6%」這條動手後發現不適用**：等距建築的東南面天生比西南面大（實測硬像素 2460 vs 1371＝差 28%），不是歪斜。已把該條換成有意義的三條：硬像素外框不貼邊、硬像素不越過格緣、孤立碎點 ≤1%。
10. **城中截圖一開始看到「施工中的菱形」**：`GV.place` 放下去是 `age:0`（施工疊層），不是完成圖。改用 `GV.art574.plant574`（age=30）拍完成圖給業主。**另外**：想用 `GV.step(40)` 讓它長大，結果觸發瘟疫＋暴亂事件、畫面變白（事件疊層）——不要用推進來拍照。
12. **事故（已處理，如實記）**：第一次推送時打了 `git push -u origin dsk/uk-cathedral`，但本地分支的 upstream 被 `checkout -b … origin/main` 設成了 `origin/main`，`push.default=upstream` 就把 commit **直接推上 main**（紅線）。發現後立刻用 `--force-with-lease=main:2dd188e` 把 main 還原回 `3f0bbad`（lease 只允許覆蓋我自己那一筆），再用顯式 refspec `origin dsk/uk-cathedral:dsk/uk-cathedral` 推分支、並把分支 upstream 改指 `origin/dsk/uk-cathedral`。現況：`refs/heads/main=3f0bbad`、`refs/heads/dsk/uk-cathedral=2dd188e`（已用 `git ls-remote` 核對）。**副作用**：那 1–2 分鐘內 GitHub Actions 對 main 跑過一次我的 commit（可能也觸發過 Pages 部署），內容與本分支相同、無其他檔案。**教訓：這條線的 `push.default=upstream` 會讓「只給分支名」的 push 打到 upstream；往後一律用顯式 refspec，或先 `-u origin <分支>` 再推。**
11. **沒做**：只做 v0（b07 慣例的 v1/v2 變體這輪不做）；不做 2×2／3×3 巨構版（那要走 metro 巨構系統，風險與工期不同量級）；西立面「六對柱／上層四對」在 20px 寬的面上像素化為 6 根柱／4 根柱（照結構不硬抄柱數）；十字是 5px 像素十字；k179-186 的觀光累加缺口沒動（會改既有城市經濟），留給業主決定。
