# DSK-007 皇家音樂廳近景疊層（k193 的 z≥1.22 細節層）— 分支 `dsk/oval-hall`

> 分支寫入者卡。**不配 T 號**。業主長期自走令：英倫建築、美術第一、不穿模／不出怪線／不非法交疊、做完一段先查重再決定下一段。
> 開卡時間：2026-09-27（`origin/main` = `d172e97`；DSK-005 k193 已在本分支 `0aaf772`）。
> 本輪不動 `GAME_VER`／`GAME_ANCHOR`／版本字／`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md`。

## 1. 為什麼做這一段（自查 DSK-005 卡第 6 節第 1 條）

DSK-005 的「沒做成的事」第一條就是「**近景疊層沒做**」。`main` 上有這套慣例（T596 軸三、DSK-002/003/004 都做了自己的近距層），本輪把 k193 補上：
**只在 z≥1.22 疊加、不動 SPR 精靈 ⇒ 遠距剪影與指紋完全不變**，是最安全的「打磨」方式。

## 2. 規格（動手前實測）

- 掛載點三處：① 近距層註冊表（index.html:59296 的 `['187',…]` 之後）② `draw()` 內的顯式呼叫（63037 巴特西那行之後）③ 新增函式（座堂那支之後、`inPoly606` 之前）。
- 簽名與樣板一致：`drawNearZoomAlbertHallDSK7(g,o,bd,s,bx,by,z,drawA,nightDepth)`。
- **座標系修正（本輪實測發現）**：樣板的 `R(lx,ly,…)` 以「精靈底部往上量」且參考高 136 ⇒ 對 184 高的精靈，`ly = 精靈Y − 48`，**穹頂（精靈 Y 41–90）大部分落在 ly<0 畫不到**。本輪自建 `R2(lx,ly,w,hh,c,al)`：`bx+lx*z` / `by+ly*z`，**直接用精靈座標**（by＝精靈頂、bx＝左），涵蓋全精靈。
- k193 既有常數（精靈段）：`CX=68 CY=118 RX=56 RY=27 DH=40 DH0=78 DRX=36 DRY=19 DHGT=30 DY0=71`；11 樘拱窗在 dx=−50…50 每 10；門廊 `PX=92 PY=143`；飾帶高度 34–37；簷口 37–40。
- 預算閘門沿用 `detailPermit432(o.x,o.y,599,z,.62)`（掛 T599 近距層的預算槽）。
- 逃生閥：`__noNearHall193`。

## 3. 驗收條件（動手前寫定）

1. 函式存在，且原始碼含 `z<1.22`、`bd.k!==193`、`__noNearHall193` 三道閘門。
2. `draw()` 內真的有呼叫（`String(draw)` 或 `__draw_501` 查得到），且把 `nightDepth` 傳進去。
3. 近距層註冊表有 `['193',…]` 條目。
4. **near>0／far=0**：自檢內用 scratch canvas 量 z=1.3 有像素、z=1.0 與 z=1.2 為 0（z=1.2 < 1.22）。
5. **夜層與日層不同**：`nightDepth>0` 與 `=0` 兩次的像素不同（>0 差異）。
6. 只給 k193：`bd.k=187` 回 0；`age<9` 回 0；`bd.ref` 回 0。
7. 逃生閥：`__noNearHall193=true` 回 0。
8. **指紋零變動**：`node fp.js --check`（**不帶** `--expect`）→ 零變動（draw-side 疊層不碰精靈）。
9. `node smoke.js` 連跑 3 次全綠、0 console error；自檢 `albertHallNearDSK7` 掛進 `smoke.js`。
10. 樣張：近景日／夜 + 市景遠近各一，入庫 `shotsDSK7/`。
11. **不動清單**：`GAME_VER`／`GAME_ANCHOR`／版本字／`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md` 一律不動。

## 4. 施工紀錄

- **2026-09-27 量測（動手前）**：讀樣板 `drawNearZoomCathedralDSK1`（59332）、近距層註冊表（59296）、`draw()` 內呼叫點（63035–63037）。發現樣板 `R(lx,ly,…)` 的 ly 以「精靈底部往上量、參考高 136」⇒ 對 184 高的 k193，`ly = 精靈Y − 48`，**穹頂（精靈 Y 41–90）大多落在 ly<0 畫不到**；本輪自建 `R2(lx,ly,…)`＝`bx+lx*z`／`by+ly*z`（精靈座標直算），涵蓋全精靈。
- **施工**：`scratchpad/dsk7-apply.js` 6 步（近距層函式插 `inPoly606` 前、註冊表 `['193',…]`、`draw()` 內呼叫、自檢函式插 `ukLandmarkSelftest677` 前、GV 匯出清單、smoke.js 條目），每步單一命中斷言＋**54 個 script 區塊語法解析**閘門。
- **踩雷 1（自檢第一次紅）**：合成量測原本用 `o={x:40,y:40}`，被 `detailPermit432` 的槽位擋掉（樣板用的是 `x=60..67`）⇒ 改用樣板同組座標後 7 項全綠。**教訓：近距層的自檢座標必須沿用樣板可通行的那組。**
- **踩雷 2（探針空地判定）**：`tileAt613(x,y,2,2)` 的輸出寬度隨格子類型變動（鐵軌佔 2 字元）且**跨行**，`replace(/\s/g,'')==='....'` 抓不到空地 ⇒ 改成不含反斜線的 `split(' ').join('')`＋`String.fromCharCode(10/13)` 寫法。**教訓：python→JS 檔→模板字串→頁面 四層轉義會把 `\s` 吃掉變 `s`，能不用反斜線就不用。**
- **踩雷 3（本卡第 3 節第 8 條寫錯，如實更正）**：原寫「`fp --check` **不帶** `--expect` → 零變動」；實際上本分支已帶 DSK-005 的 `bld.193_1_0`，bare `--check` 必然報「新增 1」。正確期待＝**`--check --expect=bld` 且新增恰好只有 `bld.193_1_0`、別無其他**（＝本輪零指紋變動）。照此執行並通過。
- **2026-09-27 驗收結果**：
  - 自檢 `albertHallNearDSK7` **7 項全綠**：三道閘門齊、用 T599 預算槽＋R2、`draw` 內有呼叫、**near>0（z=1.3 畫 2482px）／far=0（z=1.0 與 z=1.2 皆 0）**、夜層與日層不同（夜 4203px）、只給 k193（187→0／age4→0／ref→0）、逃生閥關得掉。
  - `node smoke.js` **連跑 3 次全綠**（57.8s／50.6s／64.8s），0 筆 console error。
  - `node fp.js --check --expect=bld`：新增 1（僅 `bld.193_1_0`）、觸及僅 `bld`、指紋差 == 宣告清單、棘輪 OK（153 族）⇒ **本輪沒有新增任何指紋變動**（draw-side 疊層不碰精靈畫布）。
  - 樣張：`shotsDSK5/`（`near_day`、`near_day_zoom`、`near_night`、`near_night_zoom` 已入庫）。
- **沒動清單**：`GAME_VER`／`GAME_ANCHOR`／開始畫面版本字／`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md` 全部未動 ✓。

## 5. 沒做成的事（如實，不美化）

1. **只做了表面細節**：穹頂鐵肋、窗鉛條與拱石、飾帶嵌片、齒飾、鼓頂欄杆、門廊柱頭與圓飾、露臺鋪面縫——**都是貼圖級細節，沒有新增幾何**（沒有立體格構樑、沒有門廊雕像、沒有真正的欄杆立柱）。
2. **沒有擴到 k194**：有廊客棧的近景層沒做（它在另一條分支 `dsk/galleried-inn`，一條分支一個主題）。
3. **沒有音樂會活動**：沒有人潮、排隊、進場光束、鐘聲——夜間只有窗光、穹頂透光、門燈與近景燈柱。
4. **近景燈柱是「幻影」**：只在 z≥1.22 出現的兩盞燈，遠看不存在（這是本套近距層慣例的既有取捨，如實記）。
5. **沒有量效能**：近距層在放大時有額外繪製成本（每格 ~2500 次 fillRect 級），**沒跑 `perf.js`，沒量就是沒量**。
6. **PR 沒有自己開**：`gh` 這個帳號非協作者，照舊由業主開或指名分支。
