# DSK-009 英式炸魚薯條店（1×1 高街店面）— 分支 `dsk/chippy`

> 分支寫入者卡。**不配 T 號**。業主長期自走令：英倫建築、美術第一、不穿模／不出怪線／不非法交疊、做完一段先查重再決定下一段。
> 開卡時間：2026-09-27（`origin/main` = `d172e97`；已交付未合併：`dsk/oval-hall` DSK-005+007、`dsk/galleried-inn` DSK-006+008）。
> 本輪不動 `GAME_VER`／`GAME_ANCHOR`／版本字／`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md`。

## 1. 查重（動手前）

503 筆建造工具名核對：**炸魚／薯條／fish／chip／Chippy 全部 0 命中**；最接近的是「小吃」6 筆（節慶／市場類的字串），沒有店鋪型建築。電話亭、郵筒已是街道細節（不做）；客棧型旅宿、茶室、書店、藥房仍未做（留給後續）。**本卡題材不重複，而且是本城第一間「小吃店」型店面。**

## 2. 取材（真實世界）

英式炸魚薯條店（British fish and chip shop）：**白釉磚店面＋深藍／紅色飾帶**、大面**外帶窗口**（夜裡透出炸鍋暖光）、**不鏽鋼抽風管（flue）沿牆而上並在屋頂帶防雨罩**、門上小遮雨篷、**黑板菜單立牌（A-board）**、排隊長凳、外帶紙包。樓上通常是磚造住宅層＋紅磚煙囪。典範為倫敦與英格蘭海濱城鎮的高街店面（Borough／海濱一線）；事實為通識性店面特徵，聯網檢索未回傳可引用網址故不列連結。

## 3. 規格（動手前實測）

- 新 k 值：**k195**（`origin/main` 最大＝192；193/194 在我未合的兩條分支上 ⇒ 本卡用 195 以免與自己衝突）。
- **1×1 慣例（照 k187 聖保羅座堂的地標世代，不用 T335 店鋪世代）**：`W=72,H=112,ax=36,ay=110`；`plate(base.g,ax,ay,…)`＝菱形中心 `(ax, ay-32+16)=(36,94)`、半寬 32、半高 16（實測公式）；幾何 `G=GP(ax,ay)`（`[ax+2i-2j, ay-32+i+j-z]`）；收尾 `finish(K,v,base,st,nt,ax,ay,OL)` **吃同一套 outline／polish526／mat479 後製**（T335 世代是三代畫布直寫、不吃後製，外觀會與城市不一致 ⇒ 不採用）。
- 註冊點（1×1 店鋪樣板，取自 `bank`／`post`）：`KNAME`／`COST.chippy`／`TOOLS{id:'chippy',cat:'svc',ic:'🍟'}`（**無 unlockRank**，店鋪不設解鎖階）／`LMCFG309[195]`／`placeCost` switch／`doPlace` case／`catalogKeywords458`／觀光累加白名單；**`canPlace` 的 case 群一定要加**（該 switch 沒列到就落到 `return '無法建造'`，這是 T658 的坑）。
- 不進 `MSZ`（1×1）、不進 `toolSize458`（預設 1）、不需 `canPlaceMulti` 守衛。
- 成本／地標數字（估）：`COST.chippy=1400`（小店；對照銀行 2600、民宿 1800）；`LMCFG309[195]={t:10,j:6,u:3}`。
- 逃生閥：`__noChippy195`（不烘精靈）／`__noChippyNight195`（不畫夜圖）。

## 4. 驗收條件（動手前寫定）

1. 精靈 `SPR.bld['195_1_0']` 存在、72×112、錨點 (36,110)；實心像素不貼畫布邊。
2. **台基菱形越界 0**：每欄最低實心像素 ≤ `rim = 94 + 16*(1-|x-36|/32)`（超出＝壓到鄰格）。
3. 美術像素斷言（不靠眼睛）：白釉磚 >400、紅磚 >200、夜間店內暖光 >80。
4. 夜圖是疊層不是複製（差異像素 >60）。
5. 註冊：`KNAME[195]='炸魚薯條店'`、`COST.chippy===1400`、`TOOLS` 有 `chippy`（`cat:'svc'`）、`LMCFG309[195]` 三項 >0、`toolSize458('chippy')===1`。
6. `canPlace('chippy',…)` 在空草地回 true；在水面／道路回訊息字串（非 true）。
7. `doPlace('chippy')` → 該格 `{k:195,lv:1}`、四鄰 bld 不變。
8. 放完 `draw(.016)` 不擲錯、不用 `console.error` 兜。
9. 觀光白名單含 195；目錄關鍵字含「炸魚」「薯條」「倫敦」。
10. 自檢 `chippySelftestDSK9`（≥12 項）掛進 `smoke.js`；`node smoke.js` 連跑 3 次全綠、0 console error。
11. `node fp.js --check --expect=bld` → 新增恰為 `bld.195_1_0`、其餘 152 族零變動、棘輪 OK。
12. 樣張：精靈日／夜＋市景近拍，入庫 `shotsDSK9/`；不動清單（`GAME_VER` 等）一律未動。

## 5. 施工紀錄

- 2026-09-27 查重＋量測（1×1 慣例、`plate` 菱形公式、canPlace switch 陷阱、`bank`/`post` 註冊樣板）完成。

## 6. 沒做成的事

（施工中；結案前必填，不准空著、不准美化。）
