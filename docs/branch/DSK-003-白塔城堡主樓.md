# DSK-003 — 倫敦塔白塔式城堡主樓（英式地標）

**卡片號**：DSK-003（分支寫入者不佔 T 號；合併時由 main 寫入者配 T 號）
**基線**：`0ba9d78`（DSK-002 完成後的 `dsk/uk-cathedral`）
**分支**：`dsk/uk-cathedral`（同一分支續做，座堂已在此分支）
**狀態**：施工中
**來源**：業主 2026-09-26 目標「在 branch 自己找內容做，取材倫敦或英國真實街景；最低新增一個建築，把建築打磨好」＋助手宣告的下一段。

## 0. 查重（不重做）

| 已經有的 | 不相撞的理由 |
|---|---|
| k187 聖保羅座堂（本分支） | 圓頂／尖塔／洋蔥頂，全是**高瘦宗教建築** |
| k67 鐘樓／k99 婚禮教堂／k66 信仰中心 | 單塔／小教堂，不是城堡量體 |
| k31 監獄／k42 市政廳 | 現代或古典 civic，不是諾曼式軍事建築 |
| 同期別人：GROK-002 倫敦馬廄街屋 | 街屋立面，非地標 |

⇒ **k=188「白塔」**（諾曼城堡主樓）是缺口，剪影刻意與座堂相反：**矮胖、方正、白石灰岩、四角塔＋城垛**。

## 1. 取材（真實世界特徵，照結構做）

倫敦塔 White Tower（1078 起，Gundulf 設計，Caen 白石灰岩）：方形主樓約 36×32 m、四角各有方形角塔（東北角塔圓頂、含螺旋梯）、頂部城垛（crenellations，垛與缺口交替）、牆面開箭窗（狹長十字縫）與少量拱窗、屋頂平緩、南面有門廊。本輪只取**結構特徵**（比例、四角塔、垛口、箭窗），用本線既有等距像素語彙重畫；不描摹任何遊戲。

## 2. 改什麼／不改什麼

**改（只加不改，同 k187 流程）**：新 1×1 地標 **k=188**、tool id `keep`，`$4800`、`unlockRank:17`；精靈 `SPR.bld['188_1_0']`（72×136、`ax=36 ay=134`）畫在 `b07` 的 `@@NEXT@@` 位；夜圖（箭窗暖光＋塔頂燈）；近距細節層 `drawNearZoomKeepDSK1`（`z≥1.22`／`detailPermit` T599 槽／`__noNearKeep188`）：垛口逐顆加密、箭窗十字縫、石縫橫紋。九處註冊同 k187（KNAME／COST／TOOLS／LMCFG309／placeCost／doPlace／canPlace／catalogKeywords／觀光累加）。

**不改**：既有建築與註冊；存檔結構；`GAME_VER`／`GAME_ANCHOR`／`AUTORUN-LOG.md`／`fp.json`／`DECISIONS.md`。

## 3. 怎麼算做完（動手前寫下）

1. 自檢 `keepDSK3`（掛 smoke）：精靈存在且幾何正確、白天不透明像素 1200–6000、夜圖亮像素 >50、孤立碎點 ≤1%、硬像素不越界（x∈[2,69]、y≤134）且不貼邊、九處註冊齊、`doPlace('keep')` 放得下去且八鄰 bld 不變、近距層有 z≥1.22 閘門且只給 k188、draw 內有呼叫、夜 z=2 有畫／夜 z=0.6 = 0px。
2. 城中實測：種子城放置一座 → 四鄰仍是空地（不非法交疊）＋近景樣張。
3. 煙霧 3 連綠；`fp.js --check --expect=bld`（新增葉只在 `bld`）。
4. 卡面施工紀錄如實（含失敗）。

## 4. 施工（實際改了什麼）

- **精靈**：b07 的 @@NEXT@@ 位（k187 之後）新增 k188 區塊：石鋪前庭＋石徑、台基、5.4 單位見方的主樓（Caen 白石、兩道腰線）、東南／西南兩面各 3 組箭窗（十字縫）＋南門、頂面城垛（每 2 單位一顆垛）＋鉛頂、四角方形角塔（高度 hK+16、塔頂垛口、後二先畫＝正確遮擋）、南角塔旗桿＋紅旗、前庭雙燈；夜圖 112 個暖光亮像素（箭窗＋門＋角塔＋塔頂）。零 R()。逃生閥 __noKeep188／__noKeepNight188。
- **九處註冊**：KNAME[188]=白塔、COST.keep=4800、TOOLS(keep,culture,🏰,rank17)、LMCFG309[188]={t:30,j:8,u:10}、placeCost switch、doPlace（k:188,lv:1,v:0）、canPlace 地標 case 群、catalogKeywords（城堡／主樓／倫敦塔／白塔／諾曼／城垛／箭窗）、觀光累加納入 188。
- **近距細節層** ：接在座堂層之後（draw 內的疊畫鏈）、傳入 nightDepth；只在 z≥1.22（T599 預算槽、逃生閥 __noNearKeep188）。日：石縫橫紋 7 道＋箭窗十字縫＋垛口逐顆加密；夜：箭窗暖光＋塔頂燈列 3 盞。
- ：自檢清單加 。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| （煙霧常駐，**12 項全綠**） | 精靈 72×136／ax36／ay134；白天不透明 2481；夜圖亮像素 112；孤立碎點 1（0.04%）；**硬像素越界 0**；外框不貼邊；九處註冊齊； 真的放得下去且 **t.bld.k=188**；**只佔自己那一格（八鄰逐一同值）**；近距層 z 閘門（日 z2 200 筆／406px、z0.6 0px；夜 z2 466px、夜 z0.6 0px）；draw 內有呼叫 |
| 煙霧 | **綠 3／紅 0**（30.2／31.1／31.2s）； 20 項照樣綠 |
|  | **新增 4 葉**（＋）／變更 0／移除 0，**只觸及  一族**； 不變；七軸棘輪 OK |

樣張：待下輪用 probe 拍（ 系列同款流程）。

## 6. 施工紀錄（如實，含失敗）

1. **南角塔越界 6 個硬像素**（外框 y 到 135，格線只到 134）：塔角放在 G(17,17)＝(36,136)，整個主樓偏南 2 單位。修法：主樓整體北移 2 單位（SB 由 G(14,14)→G(12,12)、四塔 [9,9]/[15,15]/[9,15]/[15,9]、旗桿跟著移），近距層的既有座標同步 -4px。**這是自檢「硬像素不越界」抓到的**。
2. **patch 腳本把檔案寫壞一次（已還原）**： 會解讀替換字串裡的  模式——我的道具價格片段含 （）， 是「match 之後的全文」⇒ 整個檔案被複製膨脹、15 個錨點全變成 3 個命中。 還原到上一個綠點，改用**函式替換**（）重跑，15 個錨點全中。教訓：批次改檔一律用函式替換。
3. **LMCFG309 的第一版錨點沒中**：原錨點帶長註解字串，改成插在 ",186:{t:12,j:1,u:2}," 之後就穩定命中。
4. **沒做**：白塔只做 v0（沒有 v1／v2）；沒有城中樣張與近距對照圖（數字齊、圖待補）； 的 DeepSeek 列名仍待業主決定。

## 7. 撤回（2026-09-26，如實記）

**這一版的程式碼已整段退回上一顆綠點 `0ba9d78`（只留卡面）**，原因：

- 煙霧自檢 `keepDSK3` 12 項全綠（精靈幾何／邊界／碎點／註冊／doPlace 放置／近距層 z 閘門），但那些檢查**都沒有把白塔真的畫到畫面上**。
- 用 `probeDSK2.js --mode=keepzoom` 真的放一座進城、呼叫 `forceDraw()` 時擲錯：
  `TypeError: Cannot read properties of undefined (reading 'sc')` at `draw`（index.html:61935 的 `if(s.sc!==undefined)`）
  ⇒ 該格建築在繪製鏈中被某個分支換成了 `undefined`。
- 對照：k187 座堂走同一條路徑沒有問題（近距層 A/B 量到 635px、遠距 0px），所以差異在 k188 自己（少註冊某張表或某個分支不吃這個 k）；**尚未定位，不在這一輪猜著修**。
- 教訓（寫進下一輪驗收條件）：**「放得下去」不等於「畫得出來」**——放置類自檢必須再加一步「放置後真的 `forceDraw()` 一次且 console 乾淨」，否則守衛有盲區。

### 7b. 追查進度（2026-09-26，k187 對照）

已排除的嫌疑（實測）：
-  有 116 個成員，**不含 187、也不含 188**； 回 **0**。
  ⇒  這條**k187 也會走**，而 k187 活得下來
  ⇒ ** 不是 k188 獨有的差異點，排除**。
- 下一步（下一輪第一件事）：在  建築段把  與每一步覆寫後的  印出來，用「k187 vs k188 兩格並排」的方式二分找出那個分支；找到後修法有兩種：讓該分支放行 k188，或讓 k188 不要進那條分支。
- 已落地的一步：k187 的自檢  加了「近距層對 k187 呼叫（日＋夜、z=2.4）不擲錯也不用 console.error 兜」⇒ **21 項全綠**（k188 修好後同樣要加）。

### 7c. 結案（2026-09-26）：白塔復活、圖拍到、教訓進了兩座地標的自檢

- **修復**：診斷 log 顯示進城時 s=ok72、DrawOK ⇒ k188 註冊本身沒問題，先前的 TypeError 是「patch 只套用一半」的殘留工作樹；程式重新套用並在 a3d3eae 首次真正進版控（前一個 commit 76c8771 內含 0 行 k188）。
- **實景與近距對照**：probeDSK2 --mode=keepzoom → 近距 near=461px／遠距 far=0px；樣張 6 張（keep_near、keep_near_zoom、keepzoom on/off、夜間 on/off）在 shotsDSK2/。
- **教訓落地（兩座都做）**：cathedralDSK2 21 項、keepDSK3 13 項，新增項是「在鏡頭附近真的放一座、真的 draw(.016) 一次、不擲錯也不用 console.error 兜」——**走真實 draw 路徑**，這才是這次盲區（只驗精靈與註冊、沒驗畫得出來）的正解。
- **守衛**：煙霧 3 連綠（31.2／31.2／30.9s）；fp --check --expect=bld → 新增 4 葉（bld.187_1_0/_1/_2 ＋ bld.188_1_0）、變更 0、移除 0、只觸及 bld、block559 不變、棘輪 OK。
- **待補**：白塔 6 張圖尚未目視；白塔沒有 v1／v2；city 實景之外還沒有夜間全景樣張。
### 7d. 目視與微調（2026-09-26）

- 目視 keep_near_zoom（白天近距）：讀得出白塔＝矮胖方主樓＋四角塔＋城垛＋箭窗十字縫；**箭窗與十字縫都坐在牆面上、沒有偏移**（近距層的石縫／垛口也在正確位置）。
- **發現的缺點**：四角塔原本比主樓高 16px，遠看像四片立鰭、把主樓頂的城垛壓掉。修法：塔高 hK+16 → **hK+7**（連旗桿高度一起改，共 4 處）。
- 重跑：煙霧 3 連綠（31.8／31.3／31.5s；cathedralDSK2 21 項、keepDSK3 13 項）；fp 仍新增 4 葉、只觸及 bld、棘輪 OK；keepzoom 樣張重拍（near=461px／far=0px）。
- 待補：夜間樣張只看過一組、還沒逐張比對；白塔無 v1／v2。
### 7e. v1 圓塔版（2026-09-26）

- **v1**：主樓與三座角塔同 v0，**東北角塔改圓柱＋鉛圓頂**（真實 White Tower 東北角塔是圓頂收頭、內有螺旋梯），圓塔身開 4 個 1px 箭窗（夜間發亮）。
- **變體選擇**：doPlace 的 case keep 依 streetHash(x,y,1881) 選 v0（<.55）／v1，只動這一座。
- **自檢**：keepDSK3 由 13 項擴到 **17 項**——兩變體都存在同幾何且有夜圖、兩變體像素簽章不同、兩變體都過同一組邊界與碎點檢查、選擇器 60 座標抽到 {0,1} 兩種；原有 13 項（含放置後真的 draw 一次）照樣綠。
- **守衛**：煙霧 3 連綠（30.1／30.9／31.1s）；fp --check --expect=bld 新增 **5 葉**（bld.187_1_0/_1/_2 ＋ bld.188_1_0/_1）、變更 0、移除 0、只觸及 bld、block559 不變、棘輪 OK。
- 待補：v1 的城中樣張尚未拍（下一輪用 keepzoom 拍 v1 一組）。