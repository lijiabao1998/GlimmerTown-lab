# DSK-008 有廊客棧近景疊層（k194 的 z≥1.22 細節層）— 分支 `dsk/galleried-inn`

> 分支寫入者卡。**不配 T 號**。業主長期自走令：英倫建築、美術第一、不穿模／不出怪線／不非法交疊、做完一段先查重再決定下一段。
> 開卡時間：2026-09-27（`origin/main` = `d172e97`；DSK-006 k194 已在本分支 `715a285`；DSK-007 已在 `dsk/oval-hall` `cb59783` 做過同一套手法的 k193 版）。
> 本輪不動 `GAME_VER`／`GAME_ANCHOR`／版本字／`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md`。

## 1. 為什麼做這一段

DSK-006 卡第 6 節第 1 條＝「近景疊層沒做」。DSK-007 已在 `dsk/oval-hall` 把同一套手法套到 k193 並驗證可行（自檢 7 項、煙霧 3 連綠、**指紋零變動**）；本輪把 k194 補上。**只在 z≥1.22 疊加、不動 SPR 精靈 ⇒ 遠距剪影與指紋完全不變。**

## 2. 規格（動手前實測，座標取自 DSK-006 v5 精靈段）

- 掛載點三處（與 DSK-007 相同）：近距層註冊表（`['193',…]` 之前/之後）、`draw()` 內呼叫（`drawNearZoomAlbertHallDSK7` 那行之後）、新增函式（插在 `drawNearZoomAlbertHallDSK7` 之前）。
- 座標系：用 **R2＝精靈座標直算**（`bx+lx*z`／`by+ly*z`）。樣板的 `R()` 以精靈底部往上量、參考高 136，對 184 高精靈畫不到上半部（DSK-007 已記錄）。
- k194 既有常數：`CX=68 CY=118 WT=56`；立面 `gxL(d)=95-d`、`gyL(d)=158-((d+1)>>1)`、`NL=26`、`BRICK_D=26`；垂直排列＝勒腳 0–5／一樓窗 5–14／層帶 14–16／下廊 15–24（走道板 16–19、欄杆 19–24）／二樓窗 26–34／上廊 35–44（板 36–39、欄杆 39–44）／三樓窗 46–52／橫飾板 52–56；木多立克柱 d∈{27,33,39,45}（16–35）；吊燈 x∈{65,57,49}；山牆大門 `ad=6`；南翼 `edge(x)=150+(x-4)/2`、車門拱 x=32–39；酒桶 x=46／56；長凳 x=20；燈柱 x=57。
- 預算閘門沿用 `detailPermit432(o.x,o.y,599,z,.62)`；逃生閥 `__noNearInn194`。

## 3. 驗收條件（動手前寫定）

1. 函式存在，原始碼含三道閘門 `z<1.22`、`bd.k!==194`、`__noNearInn194`，且用 `detailPermit432` 與 `R2`。
2. `draw()` 內真的有呼叫（`String(draw)` 或 `__draw_501` 查得到）。
3. **near>0／far=0**：自檢內用 scratch canvas 量 z=1.3 有像素；z=1.0 與 z=1.2（門檻下緣）必須為 0。**座標沿用樣板可通行組 `x=60..67,y=60`**（DSK-007 的教訓：用 `x=40` 會被預算槽擋掉）。
4. **夜層與日層不同**（`nightDepth>0` 與 `=0` 像素數不同）。
5. 只給 k194：`bd.k=193` → 0、`age<9` → 0、`bd.ref` → 0。
6. 逃生閥：`__noNearInn194=true` → 0。
7. **指紋**：`node fp.js --check --expect=bld` → 新增恰為 `bld.194_1_0`（DSK-006 帶來的），**本輪不得新增任何葉子**、棘輪 OK。
8. `node smoke.js` 連跑 3 次全綠、0 console error；自檢 `georgeInnNearSelftestDSK8` 掛進 `smoke.js`。
9. 樣張：近景日／夜入庫 `shotsDSK8/`，肉眼確認不穿模、無怪線。
10. **不動清單**：`GAME_VER`／`GAME_ANCHOR`／版本字／`AUTORUN-LOG.md`／`fp.json`／`docs/DECISIONS.md` 一律不動。

## 4. 施工紀錄

- 2026-09-27 卡面（驗收條件動手前）＋本分支遠端同步確認。

## 5. 沒做成的事

（施工中；結案前必填，不准空著、不准美化。）
