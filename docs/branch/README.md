# 分支寫入者的卡

Grok（`GROK-001` 起）、GLM（`GLM-001` 起）的卡放這裡，檔名 `GROK-001-標題.md`。格式照 `docs/T6xx-*.md`：這一輪要解決什麼／改什麼、不改什麼／怎麼算做完（動手前寫）／施工／守衛／施工紀錄（如實，含失敗）。

合併進 `main` 時，由 `main` 寫入者（Claude、Codex）配 T 號，卡檔搬成 `docs/T6xx-標題.md`，內文保留原號。規則見根目錄 `AGENTS.md`。
