# T670 — 工業區廠房的屋頂和牆換顏色

**輪次**：r117
**基線**：`ce0af82`（v13.74／T669）
**狀態**：已落地（v13.75）；卡面先單獨提交 `3365e63`
**業主決定**：2026-09-26 決策單第七批「工業區一整排廠房長得幾乎一樣」選 **C 屋頂和牆都換**（`docs/DECISIONS.md`）。

---

## 1. 這一輪要解決什麼

工業街區（k=3）的配色 `metroPalette516` 只有 6 組偏灰的牆色；屋頂 lv1 是牆暗色加深、lv2 以上從 6 個深灰深褐裡挑，一整片像同一棟。

## 2. 改什麼／不改什麼

**改**（照決策單上拍的 C 檔，同一組公式）：
- 工業屋頂從 8 色輪替：鏽紅 `#8c4a36`、灰綠 `#4f7466`、淺灰金屬 `#a9aeb0`、米色水泥 `#b8a888`、藍灰 `#4f6f8f`、深鏽 `#7a3e2e`、灰 `#9aa39c`、沙色 `#c2b59a`；第 `(v×7＋lv×3＋we) mod 8` 個。
- 工業牆（亮／中／暗三階）從 6 組輪替：紅磚、米白、淺藍鐵皮、沙色、水泥灰、赭紅磚；第 `(v×5＋lv) mod 6` 組。
- 逃生閥 `window.__noIndVariety670`＝回到原本配色；街區精靈快取鍵帶閥門（`_iv0`）；72px 舊圖集在開機時建，閥門開機前設才有效。

**不改**：工業的形狀、鋸齒屋頂、煙囪、窗、夜燈；住宅、商業的配色；超街區切分。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `indVariety670`**（煙霧常駐）：工業 v=0..11、lv=1..3 的屋頂顏色至少 6 種、牆亮色至少 5 種；關閥門時等於原本的配色；住宅、商業的配色開關閥門逐一相同；快取鍵帶閥門。
2. **像素**（`probe670.js`，種子城工業區 (44,30) z=1、(40,30) z=2 正午，關紅綠燈）：新舊有差異；街區精靈抽樣——工業（k=3）每張都變、住宅與商業（k=1、2）逐像素不變。另存新舊各兩張。
3. **煙霧** 3 連跑綠；GitHub Actions 綠；`fp.js --check`：只宣告會動的家族（預期 `block559`，工業 72px 舊圖集若用同一個配色則加 `bld`），基線只併這些。

## 4. 施工

- `index.html`：`IND_ROOF670`（8 色）、`IND_WALL670`（6 組）；`metroPalette516` 工業（k=3）且沒關閥門時照公式換屋頂與牆三階；街區精靈快取鍵加 `_iv0`；自檢 `indVarietySelftest670`（掛 `GV`）；v13.75／T670。
- `smoke.js`：自檢清單加 `indVariety670`。
- 新增 `probe670.js`。
- `fp.json`：`bld` 42 葉（工業 72px 舊圖集 `bld.3_*`，以及同樣用工業配色的特殊工業建築 `bld.122_*`、`bld.123_*`）＋ `block559` `635794cb → 582c3a35`；`bld` 家族 CRC 用頁面同一鏈式算法以 Windows 葉子重算（`6c95cb42`，先驗證同算法重算 Linux 新值一致）；登記環境差的 4 葉照舊。
- `style.json`：`bld` 總分雲端量 .9612 → .9610（−.0002），Windows 基線 .9611 照同樣差值存成 **.9609**（直接存雲端值會讓業主 Windows 上的棘輪差 .0001 誤紅）。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `indVariety670`（煙霧常駐，4 項） | 工業屋頂 8 種、牆亮色 6 種 ✓、關閥門回原配色 ✓、住宅商業配色 72 組開關閥門逐一相同 ✓、快取鍵帶閥門 ✓ |
| `probe670.js` 街區精靈抽樣（k=1/2/3 × lv1–3 × v0–5，2×2） | 工業（沒有物種配色的）**10/10** 張變；T601 物種配色的工業原型（mill、warehouse、stack、mill3、tankfarm、chimneyHall）0/8 張變（同決策單 C 的圖）；住宅、商業 **0/36** 張變 |
| `probe670.js` 畫面 | 工業區 (44,30) z=1 差異 100,194 px、(40,30) z=2 差異 214,066 px |
| 跟決策單 C 的圖比 | (40,30) z=2 同鏡頭**逐像素相同**（0／846,080 px 不同） |
| `fp.js --check --expect=bld,block559` | `bld` 變更 42 葉＋超街區；其餘家族零變動；併入基線後 `--check` 全綠 |
| 煙霧（雲端） | **綠 3／紅 0**，29.8–31.2s |

樣張：`shots670/T670_{mid,near}_{new,old}.png`。

## 6. 施工紀錄（如實，含失敗）

- **守衛第一版紅**：工業街區精靈抽樣只有 10/18 張變。查出來是 T601 `speciesPal601` 對 mill、warehouse、tankfarm、stack、chimneyHall 等原型用固定配色、蓋過 `metroPalette516`。決策單 C 的實驗也只改 `metroPalette516`，這幾種在圖上本來就沒變（逐像素比對證明），所以照業主看的圖做，守衛改成「沒有物種配色的都變、有物種配色的照舊」。
- **影響範圍比卡面寫的多一點**：同一個工業配色也用在 72px 舊圖集的工業葉子和兩種特殊工業建築（k122、k123）；決策單的實驗一樣改到了它們。
- **沒做**：物種配色的那幾種工業原型沒有跟著輪替；夜裡工業窗燈顏色不變。

