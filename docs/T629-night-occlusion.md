# T629 — 夜間燈光穿牆：後面建築的窗燈畫在前面建築上

**輪次**：r76
**基線**：`736ef32`（v13.34／T628）
**狀態**：✅ 綠燈（卡面先單獨提交 `9eda9ef`，驗收條件寫在動手之前；施工中抓到並修掉一個自己造成的回歸，見 §6）

---

## 1. 這一輪要解決什麼

Pages「住宅近景・夜晚」（種子城 (12,12)，z=2，午夜）裡：

- 兩棟高樓之間的空橋，夜裡變成一大塊淡色方塊，下方高樓立面散落一片淡色小方塊，**不對齊這棟樓自己的窗格**；
- 右邊紅磚旅館的**屋頂**上，夜裡出現一排排亮方塊（白天屋頂只有幾個老虎窗）；
- 一條淡色橫線橫跨旅館上方，白天沒有。

進場探針（暫存腳本；攔 `screen` 疊色階段的繪製，再用圖片身分反查是誰的圖）：

- 橫線那一帶畫的是 k34／k105／k106 摩天樓的夜圖（`SPR.bld.34_1_1.night`、`105_1_1.night`、`106_1_2.night`）——這些樓在旅館**後面**，白天被旅館擋住，夜裡它們的燈帶直接疊在旅館上。
- 空橋那一帶有 13 個 T446D 窗燈小方塊（`rgba(255,224,156,.66)`），加上後面街區精靈的夜圖，同樣穿透到高樓立面上。
- **根因**：夜間燈光（`nightSprites`）是在所有物件畫完、晝夜 `multiply` 壓暗之後，才用 `screen` 一次疊上去的（62280 行），沒有深度、沒有遮擋。單棟建築、摩天樓、多格公共建築的夜圖、T446D 窗燈、招牌、路燈光暈都走這一層，所以後面建築的燈會穿過前面的樓。
- 超街區（T555 分條）例外：它的夜圖在物件層裡、裁切範圍內畫，所以有遮擋；但也因此被晝夜 `multiply` 壓暗（這是另一個問題，見 §2「不改」）。

## 2. 改什麼／不改什麼

**改**

1. 夜光先畫進一張離屏光層（`nightLayer629`，與主畫布同大），最後照原本的時機、用 `screen` 疊上主畫布。
2. 物件層每畫一棟建築（含超街區每一條、T596 地塊裁切、T259 施工切片），就記下一筆「遮擋」：它的圖、位置、同樣的裁切、深度。
3. 每一筆夜光記下深度：物件層裡推的＝推它的那個物件的深度；物件層之前推的路面燈光（`rdecGlow`）＝所在路格的深度；物件層之後推的（天空）＝不被擋。
4. 光層依深度順序處理：遇到遮擋就用它的輪廓 `destination-out` 擦掉光層上已經有的燈（＝它後面的燈），遇到夜光就畫上去。
5. 效能：遮擋只在它的範圍碰得到已經有燈的地方才畫；白天（沒有夜光）完全不做。
6. 逃生閥 `window.__noNightOcc629` 退回原本的單次疊加。
7. 自檢 `nightOcc629` 掛進煙霧。版本 v13.35／T629。

**不改**

- 夜光的顏色、亮度、數量、閃爍規則；夜圖本身（指紋不動）。
- 超街區的夜圖仍在物件層裡畫、會被 `multiply` 壓暗，看起來比單棟建築暗（左邊整排白色聯排夜裡幾乎看不到燈）。要不要把它們也搬進光層、變亮，是全局觀感，另出檔給業主挑。
- 樹、車、其他非建築物件不當遮擋。

## 3. 怎麼算做完（動手前寫下）

1. **自檢**（`nightOcc629`，煙霧常駐）：用兩塊合成的「建築」（後面一塊帶夜光、前面一塊不透明且蓋住它的一部分）呼叫光層合成：修正後被蓋住那一半的光層像素是 0、沒被蓋住那一半照亮；**關閉閥門時被蓋住那一半也亮**（守衛有效）。另驗前面建築自己的燈不會被自己擦掉、天空的燈不被擋。
2. **畫面守衛**（`probe629.js`，種子城住宅近景／近景／中景・夜晚）：
   - **只會變暗，不會變亮**：修正後與修正前逐像素比，沒有任何像素的 RGB 總和變大超過 3（遮擋只會拿掉燈）。
   - **管線等價**：修正後的光層但**不做遮擋**，與修正前逐像素差異 ≤ 1%（只允許重疊燈光的四捨五入差）。
   - 探針 §1 的三個位置（空橋下方、旅館屋頂、橫線）修正後那些亮方塊與橫線消失；修前修後各一張給業主。
3. **效能**：夜晚三個鏡頭各 `forceDraw` 20 次，修正前後的中位數耗時；增加不超過 25%（超過就在 §6 寫明數字並提出取捨）。白天鏡頭耗時不變（±5%）。
4. **煙霧**：雲端 3 連跑綠；GitHub Actions 綠；Pages 自動部署。
5. **指紋**：`fp.js --check` 與 T628 相同（只有環境差 4 葉；夜光是執行期疊加，不在 SPR）。

## 4. 施工

- `index.html`
  - `draw()`：`nightSprites` 的 `push` 包一層，替每筆夜光記深度 `dep629`（物件層＝推它的物件；超街區分條＝**該街區最前面那一條**；物件層之前推的路燈光暈 `rdecGlow`＝所在路格深度＋0.5；物件層之後＝不被擋）。
  - 物件層四條建築繪製分支（T259 施工切片兩種、T555 超街區分條、T596 地塊裁切、一般）各記一筆遮擋：圖、位置、同樣的裁切、當下的透明度、畫它那一條的深度 `oDep629`。
  - 超街區每一條物件多記 `blockD1629`（最前面那一條）。
  - 新增 `nightCompose629(ctx,lights,occs,a,W,H)`：燈與遮擋依深度合併；遇到遮擋就在離屏光層（`nightLayer629`）上用輪廓 `destination-out` 擦掉已經畫上的燈（同深度先擦後畫；輪廓碰不到已有燈的範圍就略過），最後把光層 `screen` 疊上主畫布。每筆燈的畫法、`grid487` 斷電熄燈、透明度照舊。
  - 夜光段：有遮擋紀錄就走 `nightCompose629`，否則走原本的迴圈。
  - 逃生閥 `window.__noNightOcc629` 退回原本單次疊加；測試鉤子 `window.__nightOccNoErase629`（走光層但不擦，驗管線等價）。
  - 自檢 `nightOccSelftest629`（5 項），與 `nightCompose629` 一起掛上 `window.GV`。
  - 版本 v13.35／T629。
- `smoke.js`：自檢清單加 `nightOcc629`。
- 新增 `probe629.js`：三個夜景＋一個白天鏡頭的像素守衛、雜訊底線、光層統計與耗時。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `nightOcc629`（煙霧常駐，5 項） | 被前面建築蓋住的後方燈 RGB 和 **0** ✓；沒被蓋住的照亮（559）✓；前面建築自己的燈不被自己擦（765）✓；天空的燈不被擋（765）✓；**守衛有效：不給遮擋時被蓋住的一半也亮（559）** ✓ |
| `probe629.js` 住宅近景・夜晚（z=2） | 變亮 77 像素＝**雜訊底線 77**（修正前連拍兩次也差這麼多）；**被擦掉 62,995 像素**；光層不擦 vs 修正前 0.048%；燈 1,145 筆、遮擋 411 筆（擦 386、略過 25） |
| 近景・夜晚（z=1.5） | 變亮 118＝雜訊底線 118；被擦掉 14,462 像素；管線差 0.03%；燈 1,147、遮擋 389（擦 370、略過 19） |
| 中景・夜晚（z=0.75） | 變亮 84＝雜訊底線 84；被擦掉 123 像素（雜訊 89；z<1 不畫建築窗燈，只剩 37 筆燈）；管線差 0.025% |
| 住宅近景・白天 | 變亮 0、變暗 0；夜光段根本不跑 |
| 耗時（無頭軟體繪圖，`forceDraw` 20 次中位數） | 住宅近景夜 1,748→1,746ms、近景夜 1,124→1,076ms、中景夜 1,505→1,366ms、白天 92→86ms；兩次量測在 −9%～+5% 之間跳動，屬量測雜訊，**沒有可見的額外成本** |
| 三個問題位置 | 空橋下方的淡色方塊、空橋白塊、旅館屋頂的亮方塊、橫跨旅館的橫線**全部消失**；空橋恢復成有窗燈的橋；旅館自己的青色招牌保留 |
| 煙霧（雲端容器） | **綠 3／紅 0**，29.4–29.8s，0 console error |
| `fp.js --check` | 與 T628 相同：只有環境差 4 葉（夜光是執行期疊加，不在 SPR） |
| GitHub Actions（`65ef49c`） | 煙霧 `main` [run 36026091013](https://github.com/lijiabao1998/GlimmerTown-lab/actions/runs/36026091013) 綠、分支 run 36026093995 綠；Pages [run 36026200578](https://github.com/lijiabao1998/GlimmerTown-lab/actions/runs/36026200578) 部署成功 |
| 樣張 | `shots629/T629_home_night_{cmp,crop}.png`、`T629_{lamps,center}_crop.png`（不進版本庫，已傳業主） |

## 6. 施工紀錄（如實，含失敗）

- **自己造成的回歸，施工中抓到並修掉。** 第一版旅館正面的青色招牌也不見了：T625 讓超街區只在**第一條（最後面那條）**發招牌，但招牌釘在正面牆上、由前面的條畫；第一版把招牌的深度記成第一條，同一塊街區前面的條就把它擦掉了。改成超街區分條推的燈記「該街區最前面那一條」的深度：自己的條不擦，擋在整塊街區前面的東西照樣擦。被擦掉的像素因此少了約 700（住宅近景）與 1,800（近景）。
- **§3 第 2 條的判準改寫。** 原訂「沒有任何像素變亮超過 3」，實測夜景有 77／118／84 像素變亮，但**修正前連拍兩次**也恰好差 77／118／84 像素（白天是 0）：夜裡有東西每畫一次就閃動一下，跟本卡無關。判準改成「變亮不超過雜訊底線」，數字完全相同。那個閃動沒追。
- 高樓上兩條青色直式招牌也消失了：T244 招牌只在商業建築自己的正面牆上，而白天同一個像素是高樓立面——招牌的主人在高樓後面，應該被擋。
- 後方路燈的光錐、光池原本直接畫在前面建築的屋頂上（例：平頂樓屋頂上一團橢圓光池），現在被擋掉；燈頭露出屋頂線的光暈保留，下緣沿屋頂線切齊。
- 精靈的半透明像素（烘進去的落地陰影）也會按透明度擦燈，沒有另外做「只用不透明像素」的遮罩；判斷影響很小（被陰影壓到的燈必須來自那棟樓後面），先不做。
- **沒做成的：**
  1. 超街區的夜圖仍在物件層裡畫、被晝夜 `multiply` 壓暗，比單棟建築的窗燈暗很多（左邊整排白色聯排夜裡幾乎看不到燈）。要不要搬進光層變亮是全局觀感，要出檔給業主挑。
  2. 樹、車輛、其他非建築物件不當遮擋。
  3. 夜裡連續兩次繪製會閃動 77–118 個像素的來源沒追。

