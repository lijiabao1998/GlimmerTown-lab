# T614 市政建築十六類重畫：消防、學校、大學、圖書館、郵局、救護、醫院、市政廳與法院

**基線**：`d5204ce`（T613 v13.23 ＋ 草稿檢查點）
**來源**：另一帳號的 Claude 於 2026-09-24 設計 `variants574/civ_a～h.js`，14:28 因週額度中斷，沒有整合、也沒有審完。業主同日指示本 session 的 Claude「全部做完，然後上 repo（lab）」。本卡是**接手收尾**：設計保留原作，我負責審查補輪、整合、守衛與記帳。

## 場景

市政類是城裡最常見的公共建築，還停在舊畫法（T574 的單一樣式＋兩個結構變體）。另一個 Claude 已經把八檔十六類重畫完——消防局 6、高級消防 30、消防總局 61、森林瞭望塔 95、學校 7、高中 108、大學 32、大學城 113、圖書館 14、圖書總館 41、郵局 15、救護站 28、綜合醫院 48、寵物醫院 102、市政廳 42、法院 43——每類三變體，只差最後一哩。

## 改什麼

- 八檔原樣嵌進 `<!-- T610 batches -->` 區塊（跟 T610–T612 同一條路）。
- 在八檔之前加一個極小的批次 `wave614_pre`：記下當時 `SPR.bld` 每個鍵指向的物件。之後的自檢拿它比對，證明新批次**真的換掉了**舊圖。
- 新增自檢 `civicSelftest614`（掛進煙霧測試 4b）。
- 版本 13.24／T614。

## 不改什麼

- 八檔草稿內容一字不動（接手審查的結論是全部可以收，見「審查補輪」）。
- 不動模擬、存檔、佔地尺寸（MSZ）、繪製端；只有 `bld` 家族的圖會變。

## 驗收（動手前寫）

1. `civicSelftest614` 全綠：
   - 十六類四十八張都通過 T612 共用自檢 `artWaveCheck612`：錨點與佔地、夜圖、夜光只在燈具周圍 2px 內、變體是結構差異、佔地下緣外沒有像素。
   - **新增一條「真的換掉了」**：每一類的 v0–v2 都不能還是 `wave614_pre` 記下的舊物件。這條是審查中踩到才補上的，見下文 cul_e k65。
   - 八個批次都在 `__t574` 報告裡、零例外。
   - 版本錨 ≥T614。
2. 煙霧測試綠；console 零錯誤。
3. 指紋台 `--expect=bld`：只有 `bld` 變動；棘輪 OK。
4. 效能：`perf.js` 六場景在門檻內；開機時間的增量要記錄下來。
5. 城裡實景逐類看過（`probe-civic.js`）。

## 審查補輪（接手時做的）

- **客觀自檢**：寫了 `probe-wave614.js`，把草稿注入遊戲，用 `artWaveCheck612` 逐類驗。市政十六類全部通過。
- **人眼**：八檔的日夜接觸表逐張看過（`shots614/review_civ_*_r0.png`，不進版本庫）。
  - 原作沒有審查輪的 civ_a（消防）、civ_c（學校）、civ_g（醫院）三檔都合格。
  - 中斷時正在修改的 civ_f（郵局、救護站）畫面完整，看不出改到一半的痕跡。
- **探針自己的漏洞**（在文化類 cul_e 上踩到，但這條守衛放在本卡）：`artWaveCheck612` 只驗「鍵上的圖合不合格」，不驗「是不是新圖」。cul_e 檔頭寫了 k65，程式裡卻根本沒有實作；舊的正立面圖一樣能通過自檢，探針第一輪就把它判成 OK。所以本卡的自檢加上「真的換掉了」這一條。

## 施工紀錄

- **凍結**：先把另一個 Claude 留下的 14 檔原樣 commit 成 `d5204ce`（sha256 另存），再開工。之後每一筆 diff 都分得出「原作」和「接手」。
- **整合**：`wave614_pre`＋civ_a～h 共九塊，照 T610 格式原樣嵌進 `<!-- T610 batches END -->` 之前。八檔草稿一字未改。`index.html` 從 6,763,427 bytes 增加到 7.33 MB。
- **煙霧測試綠**（21.0s）：`civic614` 11 項全過，包括「十六類都換成新圖」和「守衛有效：k1 住宅判為沒換」；其餘 4b 自檢全綠；console 零錯誤。
- **指紋** `--expect=bld`：新增 0、變更 58、移除 0，只觸及 `bld`（16 類×3，加上 10 個舊 v3／v4 別名改指向新圖），其餘 150 族零變動。超街區快取 CRC 一致，棘輪 OK（151 族）。`fp.json`、`style.json` 已重錄為 13.24／T614。
- **效能** `perf.js --check`：六場景全在 ±25% 門檻內，最大偏差是深夜 −6.1%。`perf.json` 沿用 T583 基線（跟 T612 一樣只比對、不重錄）。
- **開機成本**：八個市政批次合計約 **0.98 秒**（無頭 Chrome，`__t574.ms`）。最重的是 civ_d 大學與大學城 287 ms、civ_g 醫院 161 ms。整條變體佇列 48 批 7.3 秒、零例外。開機優化仍掛在 T618。
- **城裡實景**：`probe-civic.js` 分三組拍了 1×1、2×2、3×3／4×4 的日夜圖（`shots614/T614_city_s1～s3_*.png`，不進版本庫）。建築都坐在地塊上，跟道路與水岸的銜接正常。v0 位置顯示成其他變體是遊戲本身的設計（`vdraw574` 會依座標雜湊自動選變體），不是錯。
- **審查探針也補上同一條守衛**：`probe-wave614.js` 在第一個草稿執行前拍一張 `SPR.bld` 快照，逐類比對。對照實驗：拿凍結版的 cul_e（`git show d5204ce:variants574/cul_e.js`，`--from`）重跑，k65 判 **NG「65_1_0、65_1_1、65_1_2 仍是舊物件」**；現版判 OK。第一版探針就是在這裡把舊圖誤判成 OK 的。
- **沒做成**：
  ① 開機再多約 1 秒，這一卡沒有優化。
  ② 探針拍的夜圖仍然只是整體藍調，看不清夜燈（T612 尾巴④，未解）；夜間是以接觸表的夜欄驗的。
  ③ 我和原作是同一個模型的不同實例，這輪審查**不算獨立的第三方覆核**。
