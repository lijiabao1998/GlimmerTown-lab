# T633 — 兩個街區認領同一格：超街區切分重疊，兩張建築精靈疊在同一塊地上

**輪次**：r80
**基線**：`9642bd5`（v13.37／T632）
**狀態**：✅ 綠燈（卡面先單獨提交 `2125f03`，驗收條件寫在動手之前）

---

## 1. 這一輪要解決什麼

旋轉稽核（種子城住宅近景 z=2，rot 3）看到一棟藍色平頂樓被後面的紅屋頂民居蓋掉半邊，民居屋頂上還有一條垂直切痕。回到 rot 0 看同一區：一排小屋被藍色平頂樓從中間穿過，長條鋸齒屋頂壓在小屋上。業主截圖圈的「建築物交疊」還有這一個來源沒修（T624 修的是同一張精靈裡量體的前後順序）。

進場探針（暫存腳本，不進版本庫）：

- **逐點反查**畫出來的精靈：rot 3 那一塊是 (9,9) 1×4、(8,10) 4×1、(7,11) 4×1、(10,8) 1×4 四個住宅街區的分條互相覆蓋。其中 (9,10) 同時屬於 (9,9) 的 1×4 與 (8,10) 的 4×1。
- **根因**：`rciBlockOrigin547` 的起點規則是「左、上鄰格不是同類」，每個起點各自往右下貪婪長成矩形，**彼此不知道對方**。同類區是 L 形或十字形時，兩個起點的矩形會蓋到同一格，兩張街區精靈就畫在同一塊地上。深度排序只決定誰蓋誰，換一個旋轉就換一個贏家：rot 0 看起來是一排屋子穿過另一排，rot 3 看起來是樓被切掉一半。
- **全城統計**（種子城 924 格住商工建築）：**51 格被 2～3 個街區同時認領**（商業 lv2 26 格、住宅 lv2 12 格、商業 lv3 8 格、工業 lv1 4 格、商業 lv1 1 格）。
- 另有 368 格沒有任何街區負責畫：T603 聯排截斷和同一條起點規則留下的空格。這就是 D0「看不見的建築」，業主還沒挑檔，本卡不動。

## 2. 改什麼／不改什麼

**改**

1. 超街區切分改成**依掃描序認領**（y 先 x 後，與繪製迴圈同序）：起點規則不變；先到的街區認領它的格子，後到的街區長大時**不准伸進已被認領的格子**（正方形、往右、往下三步都一樣），之後照原本的 T603 聯排截斷、T604 工業上限、4×4 上限。
2. 切分全圖算一次、存起來（新函式 `rciPart633`），`rciBlockOrigin547` 查表。繪製迴圈、冒煙點、泡泡、招牌、`rciAbsorbed555` 全部拿到同一個切分。每次 `draw` 開頭作廢；查詢格的建築物件或類別、等級、變體與存下時不同（自檢種地、拆建）也會重算。
3. 逃生閥 `window.__noPart633` 退回各起點各自長（舊行為）。
4. 自檢 `part633` 掛進煙霧。版本 v13.38／T633。

**不改**

- 起點規則、4×4 上限、T603／T604 截斷、街區精靈本身（block559 指紋不變）。
- 沒有人畫的格子（D0）照舊不畫。後到的街區變小，會多出少數這種空格（探針估 +12 格），等 D0 挑檔後一起處理。
- 誰先到只看掃描序（y 小的先、同一列 x 小的先），不比大小。

## 3. 怎麼算做完（動手前寫下）

1. **零重疊**（自檢，煙霧常駐）：煙霧當下那座城，所有會畫出來的街區矩形兩兩不重疊（每格最多被一個街區認領）。
2. **十字形**（自檢）：種一條 1×4 直排，再種一條穿過它的 4×1 橫排（住宅 lv1）。修正後直排完整、橫排不含交叉格；**關閉閥門時兩者都含交叉格**（守衛有效）。
3. **矩形區不受影響**（自檢）：種一塊實心矩形同類區，修正前後切出來的街區逐一相同。
4. **消費端一致**（自檢）：同一個起點，冒煙點（`smokeViewSrc626`）用的寬高與 `rciBlockOrigin547` 相同。
5. **種子城**（`probe633.js`）：重疊格 51→0；列出形狀改變的街區；沒人畫的格子 368→實測值；住宅近景 rot 0／rot 3 修前修後樣張與差異像素，給業主。
6. **效能**：住宅近景 `forceDraw` 平均耗時，修前修後差距在量測雜訊內（切分每幀只算一次）。
7. **煙霧**：雲端 3 連跑綠；GitHub Actions 綠；Pages 自動部署。
8. **指紋**：`fp.js --check` 全綠（精靈沒變）。

## 4. 施工

- `index.html`
  - `rciBlockOrigin547`：起點規則不變；確定是起點後查 `rciPart633`（`__noPart633` 時不查），回傳一份複製。
  - 新增 `rciGrow633(x,y,b,k,lv,cl)`：原本的長大規則原封搬出（正方形、往右、往下、T603 聯排截斷、T604 工業上限、maxLv），`cl` 是已被認領的格子，`null` 就是舊行為。
  - 新增 `buildPart633()`：全圖依掃描序認領；`rciPart633(x,y)`：快取 `part633`，查詢格的建築物件或 k／lv／v／ref 與存下時不同、相關閥門（`valves633()`：552／602／568／602 上限／603／604）切換，就重算；起點不在表裡（別處拆建後才成立）重算一次。
  - `draw()` 開頭 `part633=null`：每次繪製第一個查詢時重算一次。
  - 逃生閥 `window.__noPart633`。
  - 新增自檢 `partSelftest633`（6 項）；GV 出口 `partSelftest633`、`part633`、`buildPart633`。
  - T602、T603 自檢的「原始碼裡讀得到 `blockMax602`／`__noTerraceRow603`」改成讀 `rciBlockOrigin547＋rciGrow633`（邏輯搬家了，檢查跟著搬）。
  - **施工中追加**：T626 自檢量測期間把畫面震動 `shakeT` 歸零、結束還原（見 §6）。
  - 版本 v13.38／T633。
- `smoke.js`：自檢清單加 `part633`。
- 新增 `probe633.js`：覆蓋統計（閥門開關各算一次）、形狀改變的街區、耗時、rot 0／rot 3 修前修後差異與樣張。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `part633`（煙霧常駐，6 項） | 當下這座城的街區兩兩不重疊（種子城 124 塊、重疊 0）✓；十字形：直排完整 1×4 ✓、橫排讓開交叉格（1×1）且重疊 0 ✓；冒煙點的錨格跟著切分走（新：橫排自己那格；舊：4×1 最前面那格）✓；**守衛有效：關閉閥門時橫排又長回 4×1、重疊 1 格** ✓；實心 4×3 矩形新舊切法相同 ✓ |
| `probe633.js` 種子城覆蓋 | 924 格住商工：**重疊格 51 → 0**；剛好一個街區 505 → 544；沒人畫 368 → 380（+12，D0 那一類） |
| 形狀改變的街區 | 多格街區 130 → 124 塊，22 塊改變：(25,3) 4×3→1×4、(32,3) 3×2→1×2、(31,4) 4×1→1×3、(14,5) 2×2→1×2、(13,6) 3×1→1×1、(46,6) 2×1→1×1、(28,9) 2×4→1×4、(8,10) 4×1→1×3、(21,10) 2×2→1×3、(31,10) 2×3→1×3、(7,11) 4×1→1×2、(19,11) 3×2→2×2、(14,21) 2×4→1×4、(13,22) 3×2→1×2、(31,22) 4×3→1×3、(46,23) 2×2→1×2、(28,24) 2×1→1×1、(45,24) 2×1→1×1、(25,27) 4×4→1×4、(22,30) 2×1→1×1、(37,33) 2×2→1×2、(16,36) 2×1→1×1 |
| 耗時（住宅近景 z=2，`forceDraw` 各 5 輪交替） | 中位數：修正前 83.3ms、修正後 80.4ms（單輪 75.8–92.1ms，差距在量測雜訊內）；每次 draw 只切分 1 次 |
| 修前修後差異像素 | 住宅近景 rot 0：111,509；rot 3：86,788（修正後連拍兩次的雜訊 0） |
| 煙霧（雲端容器） | 最終版 **綠 3／紅 0**，28.3–28.5s，0 console error（第一次 3 連跑紅一次，見 §6） |
| `fp.js --check` | **全綠**：葉子零變動（已登記環境差 4 葉）、超街區「與基線一致」、棘輪 OK（151 族） |
| 樣張 | `shots633/T633_{r0,r3}_cmp.png`（左修正前、右修正後；不進版本庫，已傳業主） |

## 6. 施工紀錄（如實，含失敗）

- **第一次煙霧 3 連跑紅一次**：`smokeView626`「煙源＝畫出來的煙囪像素」誤差 12.21px、「舊煙源 rot 0 差 74.3px（應為 64）」。追查：
  - T626 自檢種的 2×2 四周留了一圈空地，新切分不可能改到它；新舊兩個煙源一起偏，是整張畫面被平移。
  - 煙霧用的是隨機新城，碰上地震或隕石時，畫面震動（`shakeT`，z=2 最多約 15px）會把畫面平移，自檢算螢幕座標時沒扣掉。
  - 實證：先 `GV.quake()` 再跑 T626 自檢，紅成同一個樣子（13.00px、51.0px）；量測期間把 `shakeT` 歸零、結束還原後，地震剛觸發也是 0.00px。
  - 這是 T626 自檢既有的偶發紅，跟 T633 無關，施工中順手修（只動自檢）。
- T603 自檢在**種子城**本來就紅：它種的 2×2 左邊有同類鄰居，種下去那格不是起點；關閉 T633 閥門一樣紅。煙霧用的新城不受影響，沒修。
- 誰先到只看掃描序、不比大小：種子城 (25,27) 的 4×4 被上方先到的街區切成 1×4，大塊可能被小塊切薄。要改成「大的先」得另訂規則，沒做。
- 探針第一次把樣張寫到倉庫外（`/home/tmp/…`，相對路徑給錯），發現後搬回暫存區並刪掉該目錄。
- **沒做成的：**
  1. 沒人畫的格子多了 12 格（368→380），跟 D0 同一類，等業主挑檔一起補。
  2. T603 自檢在種子城的脆弱沒修。
  3. 只看了住宅近景兩個旋轉的樣張，其他 20 塊改變的街區沒逐塊看。
