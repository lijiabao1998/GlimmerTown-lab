# T639 — D0 補成建築：沒人畫的住商工格子自己長成街區

**輪次**：r86
**基線**：`a5094b9`（v13.43／T638）
**狀態**：✅ 綠燈（卡面先單獨提交佔號 `692aadb`）
**業主決定**：2026-09-25 決策單 D0 選 **B 補成建築**（`docs/DECISIONS.md`）。

---

## 1. 這一輪要解決什麼

種子城 924 格住商工建築裡，約 380 格沒有任何街區負責畫，畫面上是一塊塊草地（T633 卡面量過 368→380）。

根因（T633 卡面、本輪實驗確認）：
- 街區起點規則是「左、上鄰格不是同類」。
- T603 把住宅聯排截成最多 2 排深、商業 lv1 截成最多 2 排，T604 把工業截成最多 2×2 ⇒ 同一片區剩下的格子，左或上鄰格仍是同類，永遠當不了起點，也就沒人畫。
- 另外 T555 的「吸收」規則：1～3 格的小樓貼著同類 4 格以上的大樓就整棟不畫。

決策單的 B 檔圖，是用臨時實驗碼做的：沒被認領的格子自己當起點。業主看圖選了它。

## 2. 改什麼／不改什麼

**改**

1. `buildPart633`：起點改成「掃描序下還沒被任何街區認領的格子」（原本是左、上鄰格不同類）。長大規則（`rciGrow633`：正方形→往右→往下、T602 上限、T603／T604 截斷、不准伸進已認領格）不變。
2. `rciBlockOrigin547`：切分開著時直接查表——這一格是某塊的起點就回那一塊，不是就回 `null`；不再先用鄰格規則擋掉。
3. `rciAbsorbed555`：補格開著時不吸收（補出來的小樓要畫）。
4. `valves633` 加入新閥門，切換時切分會重算。
5. 逃生閥 `window.__noFill639` ＝ 全部回到舊行為。
6. 自檢 `fill639` 掛煙霧；版本 v13.44／T639。

**不改**

- 街區怎麼長、上限與截斷規則、街區精靈本身。
- T621 地坪、T622 旋轉、T555 分段裁切。

## 3. 怎麼算做完（動手前寫下）

1. **覆蓋**（`probe639.js`，種子城全圖，照 `probe633.js` 的算法）：沒人畫的住商工格子 **380 左右 → 0**；兩個街區搶同一格仍是 **0**。
2. **自檢 `fill639`**（煙霧常駐）：自己種一塊 4×4 住宅 lv2，補格開著時 16 格全有街區負責（應為兩塊 4×2）；關閥門時有 8 格沒人畫（守衛有效）。種一塊 4×4 工業 lv1，補格開著時 16 格全有（四塊 2×2）。量完原樣還原。
3. **既有守衛照過**：`probe621b.js`、`probe622.js` 綠。
4. **效能記錄**：住宅近景、中景 `forceDraw` 平均毫秒，修前修後各量（交替量）。增加超過 50% 就另開卡，不擋本卡。
5. **煙霧** 3 連跑綠；GitHub Actions 綠；`fp.js --check` 綠（精靈沒變）。
6. **樣張**：住宅近景、中景修前修後（業主選檔時看過同一組）。

## 4. 施工

- `index.html`
  - 新增 `fillOn639()`、`part639()`、`fillSelftest639`（放在 `rciPart633` 前）；自檢掛上 `window.GV`。
  - `buildPart633`：補格開著時，起點＝還沒被認領的格子。
  - `rciBlockOrigin547`：補格開著時直接查切分表。
  - `rciAbsorbed555`：補格開著時一律不吸收。
  - `valves633` 加 `__noFill639`。
  - `partSelftest633`：量測期間暫時關補格（見 §6）。
  - 版本 v13.44／T639。
- `smoke.js`：自檢清單加 `fill639`。
- 新增 `probe639.js`：覆蓋、效能、樣張。
- `probe621b.js`：「守衛有效對照」改用面積（見 §6）。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `probe639.js` 種子城覆蓋（924 格住商工） | **沒人畫 380 → 0**；重疊 0 → 0；剛好一個街區 544 → **924**；多格街區 124 → 235 |
| `fill639`（煙霧常駐，4 項） | 住宅 4×4 補格後 16/16 ✓、工業 4×4 16/16 ✓、關閥門時兩者都有空格（守衛有效）✓ |
| 效能（`forceDraw` 平均，新舊交替 5 輪） | 住宅近景 96.0 → 100.4 ms（×1.05）；中景 84.1 → 96.7 ms（×1.15）；在 50% 門檻內 |
| `probe621b.js` | 綠（rot 0 灰板 42,780 → 0；對照改面積後見 §6） |
| `probe622.js` | 綠 |
| 煙霧（雲端） | **綠 3／紅 0**，28.2–28.8s；`part633`、`fill639` 都 OK |
| `fp.js --check` | 全綠（精靈沒變；T631 之後雲端環境差已登記） |
| 樣張 | `shots639/D0補成建築_住宅近景.png`、`shots639/D0補成建築_中景.png`（不進版本庫；已傳業主） |

## 6. 施工紀錄（如實，含失敗）

- **煙霧第一次紅**：T633 的自檢「實心矩形新舊相同」比較切分開／關兩種結果；補格開著時，矩形被 T603 截斷剩下的格子會長出新街區，兩邊自然不同。那一項驗的是 T633 切分本身，改成量測期間暫時關補格，判準一字未改。
- **發現一個早就紅的守衛**：`probe621b.js` 的「探針 7 點舊版全是鋪面灰」是按 T621 當時佈局寫死的螢幕座標；T624／T633 之後街區變了，**在 T639 之前的版本上就已經是紅的**（這支守衛不在煙霧裡，沒人跑到）。改成用灰板面積當對照（舊版 rot 0 灰板 > 500 像素），同一支守衛原本就在量這個數字；「新版 7 點都不是灰」保留。
- 中途一度在 `probe621b.js` 加了「量測時關補格」，確認不是原因後撤回。
- **沒做的**：補出來的街區很多是 1 格寬的細長條（例如 1×4），外觀是現有街區精靈照尺寸畫出來的，沒逐塊看；遠景城市變密後的整體觀感，業主選檔時看過同一組圖。
