# T651 — 草地格線淡一點

**輪次**：r98
**基線**：`96e849b`（v13.52／T647）
**狀態**：✅ 綠燈（卡面先單獨提交佔號 `9a86c85`）
**業主決定**：2026-09-25 決策單第三批「草地格線」選 **B 格線淡一點**（`docs/DECISIONS.md`）。T648–T653 六張卡一次佔號，一張一張做。

---

## 1. 這一輪要解決什麼

每格草皮（`SPR.grass`）四邊畫了亮邊 #83c467（上兩邊）和暗邊 #66a04d（下兩邊），大片空地、河岸草坪上連成一張格子網。

## 2. 改什麼／不改什麼

**改**：地面層畫草地格時，改用「邊線往這格草色靠六成」的版本（第一次用到時由原圖產生、快取；草色取格子中央平均）。季節版、高地版同樣處理。逃生閥 `window.__noGrassGrid651`（地面快取鍵帶閥門）；自檢 `grassGrid651`；版本 v13.56／T651。

**不改**：原精靈 `SPR.grass`（留給閥門與其他用途）；沙地、水面、道路。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `grassGrid651`**（煙霧常駐）：取一張草地精靈，邊線像素和中央草色的平均色差，新版不到舊版的一半；中央像素逐像素不變；快取生效。
2. **像素**（`probe651.js`，(12,40) z=1、正午）：新舊有差異；草地上亮邊色（#83c467±4）像素新版比關閥門少八成以上。（施工時改 ±1，見第 6 節）
3. **煙霧** 3 連跑綠；GitHub Actions 綠；`fp.js --check` 綠。

## 4. 施工

- `index.html`：地面層 `base` 改 `let`，草地格（`t.t===2`）換成 `grassSoft651(base)`；地面快取鍵帶 `_g0`；新增 `GRASS651`、`grassSoft651`、`grassGridSelftest651`；`drawPath651` 在 T501 包 `draw` 之前記；版本 v13.56／T651。
- `smoke.js`：自檢清單加 `grassGrid651`。
- 新增 `probe651.js`。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `grassGrid651`（煙霧常駐，4 項） | 邊線與草色平均色差 新 18.0／舊 45.5 ✓、邊線以外逐像素不變 ✓、快取 ✓、地面層有呼叫且快取鍵帶閥門 ✓ |
| `probe651.js`（(12,40) z=1、正午，關紅綠燈） | 差異 13,446 像素；亮邊色（±1）新 18／關閥門 10,646 |
| 煙霧（雲端） | **綠 3／紅 0**，28.0–29.4s |
| `fp.js --check` | 全綠（原精靈沒動） |
| 樣張 | `shots651/T651_{new,old}.png`（不進版本庫） |

## 6. 施工紀錄（如實，含失敗）

- **守衛第一版差一點**：亮邊色用 ±4，新版剩 21.3%（門檻 20%）。草皮斑點色 #86c468 跟亮邊色 #83c467 只差 3，被算進去了；斑點在格子裡、兩版都有。改 ±1 只數邊線本身，新 18／舊 10,646。
