# T677 — 大笨鐘 1×1（k190）＋西敏宮 2×2（k191）

**輪次**：r124
**基線**：`02f880b`（v13.80／T676）
**狀態**：卡面（動手前）；T677–T681 一次佔號
**業主決定**：2026-09-26 決策單第十一批 11-1 選 **D 兩個都加**（`docs/DECISIONS.md`）。

---

## 1. 這一輪要解決什麼

業主要英倫建築「全做」。城裡已有聖保羅座堂、白塔、巴特西（都是 1×1），沒有倫敦最好認的大笨鐘。決策單拍了兩種原型（`docs/proto-b11/bigben.js`），業主兩個都要。

## 2. 改什麼／不改什麼

**改**（精靈照決策單原型碼一字不改，只把閥門拆開）：
- b07 地標模組 `@@NEXT@@` 前加 k190 大笨鐘（72×220、ax36 ay218：台基、淺黃石塔身直向飾板、金框四面鐘盤、鐘樓拱廊、深灰藍鑄鐵尖頂、Ayrton 燈、金十字）與 k191 西敏宮（136×250、ax68 ay248：東北伊莉莎白塔、垂直式哥德長立面、西南維多利亞塔掛英國旗）。閥門：`__noBigBen190`／`__noBigBenNight190`、`__noWestminster191`／`__noWestminsterNight191`（原型兩座共用一個閥，拆開；像素不變）。
- 註冊照聖保羅（k187）的做法：`KNAME`（190 大笨鐘、191 西敏宮）、`COST`（`bigBen` 3800、`westminster` 6800）、`TOOLS`（文化類、第 17 級解鎖）、`LMCFG309`（觀光／就業／維護 190：26／5／7，191：42／16／14；比照聖保羅 34／9／11、白塔 30／8／10 按價格估）、`tourLm309` 地標累加條件、價格 switch、`doPlace`、`catalogKeywords458`。
- 西敏宮 2×2：`canPlace` 需 2×2 陸地（`canPlaceMulti`）、`doPlace` 佔 2×2（根格 `sz:2`、其餘三格 `ref` 指回根格，含 undo 快照，照天文台 k68）、`toolSize458` 回 2、`MSZ[191]=2`（讀檔重建）。

**不改**：既有地標；近距細節層（聖保羅、白塔、巴特西那種 z≥1.22 疊畫）這輪不做；城市事件、市長工具池。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `ukLandmark677`**（煙霧常駐）：兩張精靈存在、尺寸錨點對（72×220／36,218；136×250／68,248）、夜圖有亮像素；註冊齊（KNAME、COST、TOOLS 文化類 17 級、LMCFG309 三項 >0、MSZ[191]=2、toolSize 2）；`doPlace('bigBen')` 在空地放得下、只佔一格；`doPlace('westminster')` 根格 k191 sz2、三格 ref 指回根格、2×2 外八鄰不變；西敏宮放在只有 1 格空地的地方 `canPlace` 擋下；放完 `draw(.016)` 不擲錯。
2. **守衛 `probe677.js`**：種子城 (48,24) 種 k190、(48,24) 種 k191（各一次遊戲），正午 z=2、午夜 z=2、正午 z=1.2 三張，跟決策單第十一批的 `bigben_{day,night,mid}`、`palace_{day,night,mid}` **逐像素相同**（原型碼一字不改的證明）；開閥門的遊戲裡兩張精靈都不存在。
3. **煙霧** 3 連跑綠；`fp.js --check --expect=bld`：只有 bld 族變、只多 2 葉（190、191），其他葉零變動；基線只併這兩葉（家族 CRC 用 Windows 葉子重算）。

## 4. 施工（預計）

- `index.html`：精靈、註冊、放置、自檢 `ukLandmarkSelftest677`、GV 出口；版本 v13.81／T677。
- `smoke.js` 加 `ukLandmark677`；新增 `probe677.js`。
- `fp.json`、`style.json`：只併 bld 新葉。
