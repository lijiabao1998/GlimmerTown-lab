# T646 — 崖沿改成深綠坡緣

**輪次**：r93
**基線**：`e095194`（v13.46／T641）
**狀態**：卡面（動手前）
**業主決定**：2026-09-25 決策單「崖沿」選 **B 改成深綠坡緣**（`docs/DECISIONS.md`）。T642–T647 六張卡一次佔號，一張一張做。

---

## 1. 這一輪要解決什麼

高低不同的地塊交界畫一道崖沿（T24 `SPR.cliffEdge`，深褐雙線＋T153 岩層線）；落差小的平地上看起來像一排棕色柵欄。

## 2. 改什麼／不改什麼

**改**

1. 新增一組深綠坡緣精靈（主線 #6f8f4e、內側陰影 rgba(40,62,30,.30)），第一次用到時才畫（不呼叫亂數）；地面層改畫這一組。
2. 逃生閥 `window.__noCliff646`＝舊的深褐崖沿；地面快取鍵帶閥門狀態，切閥門會重烘。
3. 自檢 `cliff646`；版本 v13.51／T646。

**不改**

- 哪些格子畫崖沿（`t.em` 位罩）、水岸那一套邊線。
- 舊精靈 `SPR.cliffEdge` 本身（留著給閥門用）。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `cliff646`**（煙霧常駐）：新精靈 16 張、每張的不透明像素以綠為主（G > R、G > B）；舊精靈仍以褐為主；閥門可切換。
2. **像素**（`probe646.js`，種子城有崖沿的草地近景）：新舊畫面有差異，崖沿附近的褐色像素新版明顯少於關閥門。
3. **煙霧** 3 連跑綠；GitHub Actions 綠；`fp.js --check` 綠。
4. **樣張**：崖沿近景。
