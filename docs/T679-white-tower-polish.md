# T679 — 白塔打磨：角塔鉛蔥頂＋風標、牆面扁壁柱、圓頭窗

**輪次**：r126
**基線**：T678 之後
**狀態**：卡面（動手前）；T677–T681 一次佔號
**業主決定**：2026-09-26 決策單第十一批 11-3 選 **B**（`docs/DECISIONS.md`）。

---

## 1. 這一輪要解決什麼

T659（原 DSK-003）白塔 k188「完成度中等」：平頂方角塔、十字箭窗。真的倫敦塔白塔最好認的是四座角塔頂上的鉛皮蔥形小圓頂和風標、牆面的扁壁柱、Wren 改過的圓頭窗。

## 2. 改什麼／不改什麼

**改**（照決策單原型碼 `docs/proto-b11/patch11c.js` ②，v0 一字不改；業主選 B 時說好 v1 一起改）：
- v0、v1 主樓兩面各兩道扁壁柱（d＝3、7，亮一欄＋暗一欄）。
- 十字箭窗改兩層圓頭窗（每面三開間、上層 6 高、中層 5 高、石窗台；右面中間開間讓門不畫中層窗）；夜裡上層窗亮一部分。
- 方角塔頂加鉛皮蔥頂＋金色風標（v0 四座、v1 三座方塔；v1 東北角圓塔本來就是鉛圓頂，不動）。
- 旗桿往上移 7px 立在前角蔥頂上。
- 逃生閥 `window.__noKeepPolish679`（開機時讀，精靈開機時烘）＝回原樣。

**不改**：量體、城垛、門、台基、配色、錨點與畫布（72×136）；k188 近距層（`__noNearKeep*`）——先量它有沒有畫在舊箭窗位置，有衝突再記下、另開卡。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `keepPolish679`**（煙霧常駐）：`SPR.bld['188_1_0']`、`['188_1_1']` 都在、尺寸錨點不變；兩張都有鉛色（`#8f9ca6`／`#aab6bf`）的蔥頂像素出現在角塔頂以上、有金色風標像素；舊十字箭窗的橫杆位置不再是窗玻璃色。T659 自檢 `keepDSK3` 照過（若有條件是綁舊箭窗的，改在閥門下驗、卡上記明）。
2. **守衛 `probe679.js`**：種子城 (48,24) 種 k188 v0，正午／午夜 z=2 兩張跟決策單第十一批 `keepB_{day,night}` **逐像素相同**；開閥門（preScript）的兩張跟 `keepA_{day,night}` 逐像素相同；v1 另拍近照存樣張、目視。
3. **煙霧** 3 連跑綠；`fp.js --check --expect=bld`：只有 `bld.188_1_0`、`bld.188_1_1` 兩葉變。

## 4. 施工（預計）

- `index.html`：k188 v0、v1 兩段加閥門分支；自檢 `keepPolishSelftest679`；版本 v13.83／T679。
- `smoke.js` 加 `keepPolish679`；新增 `probe679.js`。
