# T616 自走線日誌整理：最新在上、回填 pending、標出重號

**基線**：`0b72625`（T615 v13.25）　**不改版本**（純文件）

## 場景

`AUTORUN-LOG.md` 是業主醒來後判斷進度的唯一入口，但順序已經亂掉了：

- 最舊的總表（r1–r33）在最上面，中間是 r2–r26 摘要，r32–r42 的段落排在 r63 前面，r43–r63 才是新的在上；
- 有 18 列的 commit 欄從來沒有回填，一直停在 `pending`；
- r28 有一列欄位錯位的重複列；
- r32 被兩輪不同的工作共用；
- r34 寫了兩次。

## 改什麼／不改什麼

- **只重排、不改寫**任何一輪的內容，「沒做成的事」一字不動（AUTORUN 鐵則）。
- 全檔最新在上：r63→r32 的逐輪段落 → r33→r1 的早期總表 → r26→r2 摘要。
- 18 列 `pending` 依卡號從 git 歷史回填，標注「T616 回填」。T597 有兩筆 commit，兩筆都列出。
- r28 錯位列移到早期總表下方當備註，原文保留（它另有兩處細節是整理版沒有的）。
- r32 重號、r34 重複：保留原號，只加註。

## 驗收（動手前寫）

- 整理腳本自我驗證：原檔每一個非空行都還在。允許的變更只有三類：回填列、r28 備註化、r32 標題加註。
- 煙霧測試綠（不涉程式，照鐵則仍然要跑）。

## 施工紀錄

- 整理腳本在 session scratchpad（`relog.py`，不進版本庫）。第一次執行被腳本自己的驗證擋下：它發現 r32 標題「不見了」，其實是我加註造成的預期變更。把這一條列入預期後重跑：**原檔 270 個非空行全部保留，回填 18 列**。
- 回填對照：T532 `1aee856`、T533 `e974880`、T534 `1ddaf0b`、T536 `d8c3d27`、T537 `ba50ca4`、T538 `50e5934`、T539 `778e857`、T540 `1ad25bd`、T541 `20b46c8`、T548 `de4615e`、T549 `f72d262`、T550 `8f07bba`、T557 `6215e26`、T580 `601c771`、T582（interchange）`1ccfc72`、T596 `e7ec442`、T597 `d128e6f`＋`a102b81`、T598 `377b907`。
- **沒做成（日誌的缺口，只記錄、不補寫）**：
  - 日誌裡沒有 r4、r31。
  - git 裡有兩筆 commit 完全沒有日誌列：T581 bridge476 夜圖＋railStock469 盤點（`da5daf9`），以及 T583 街廓風格凝聚（`1a5e0f6`）。r31 夾在 r30（T580）和 r32（T582）之間，很可能就是 T581，但沒有直接證據。
  - T535 有卡面、沒有 commit；後來由 T540 沿用識別子 535 實作。
  - 這些都是原作者當時沒寫下的東西，我不替他補寫推測內容。
