# T628 — 街區精靈快取鍵 v%6：同一座城先看哪裡，長相就不同

**輪次**：r75
**基線**：`678ae82`（v13.33／T626，T627 工具）
**狀態**：✅ 綠燈（卡面先單獨提交 `c6ae5c4`，驗收條件寫在動手之前；施工中追加的指紋覆蓋見 §6）

---

## 1. 這一輪要解決什麼

T624 施工時發現、另開卡的問題：

- `getBlockSprite547(k,lv,bw,bh,v)` 的快取鍵用 `v%6`，產生精靈的 `makeBlockSprite547` 卻拿**原始的 v** 去挑原型（`ARCHE568`：住宅、商業 lv1–2 每級 5 種，工業 4 種）和其他細節。
- 住商工的 `b.v` 是 0–11：T150 起新建築用 `ri(12)`（T406 `pickV406` 依中心性挑，範圍相同），種子城是 `%12`。
- 所以 v 與 v+6 會落在同一個鍵，但它們本該是不同的原型（5 種時差 1 個、4 種時差 2 個）。鍵裡先放進去的那一張，兩種 v 都拿去畫。
- 後果：
  1. 大約一半的街區可能畫成別的 v 的原型。
  2. 哪一半取決於**繪製順序**：鏡頭一開始在哪、先看過哪些地方。同一個存檔重新載入、換個位置開始看，街景可能不一樣；Pages 樣張也受拍攝順序影響。
  3. `fp.js` 的 block559 指紋也跟著順序走。

## 2. 改什麼／不改什麼

**改**

1. 快取鍵改用完整的 v（`v|0`）：每個 v 一張自己的精靈。
2. 逃生閥 `window.__noVkey628` 退回 `v%6` 鍵。
3. 把 `rciBlockOrigin547` 掛到 `GV.art574.block559.origin`，給探針列舉城裡的街區。
4. 自檢 `vkey628` 掛進煙霧。
5. 版本 v13.34／T628。

**不改**

- 原型表、精靈畫法、建築拿到哪個 v、繪製迴圈用哪一格的 lv／v 取圖。

## 3. 怎麼算做完（動手前寫下）

1. **自檢**（`vkey628`，煙霧常駐）：k 1–3 × lv 1–3 × 至少兩種尺寸，每一組 v 與 v+6 都各取一次圖：修正後拿到的精靈 `__t547.v` 都等於自己的 v；**關閉閥門時至少一組拿到別的 v 的圖**（守衛有效）。
2. **順序無關**（`probe628.js`，種子城）：同一個鏡頭（住宅近景・白天），先用兩種不同的參觀順序把快取暖好再拍：修正後兩張逐像素相同；關閉閥門時兩張不同（守衛有效）。
3. **影響規模**：種子城裡「畫出來的精靈是替別的 v 做的」街區數，修正前 N 塊（其中原型或立面款式真的不同的有幾塊）→ 修正後 0。
4. **記憶體**：逛完 Pages 的 8 個鏡頭後，快取裡的街區精靈張數修正前後各多少（預期不超過 2 倍）；開機時間同一量級。
5. **煙霧**：雲端 3 連跑綠；GitHub Actions 綠；Pages 自動部署。
6. **指紋**：葉子零變動（只有雲端環境差 4 葉）；block559 指紋若變動，在暫存複本用 `--expect=block559` 確認只有它變、棘輪 OK，再只搬 `blocks.fam` 與版本欄位。
7. **樣張**：Pages 同鏡頭修前修後給業主看。這張卡是修「順序決定長相」的錯，不是換風格，但會有不少街區換成本來該有的原型，所以一定先給業主看圖；不喜歡可以用閥門退回。

## 4. 施工

- `index.html`
  - `getBlockSprite547`：快取鍵的 v 段改成 `v|0`；閥門 `window.__noVkey628` 退回 `v%6` 並另加 `_v6` 尾碼，兩種鍵不互相污染（切換閥門時不會拿到另一種模式產生的圖）。
  - `GV.block559.origin = rciBlockOrigin547`（探針用）。
  - 自檢 `vkeySelftest628`（5 項），掛上 `window.GV`；閥門模式為了驗證產生的條目，驗完從快取刪掉。
  - **施工中追加**：`GV.blockFp536`（`fp.js` 實際呼叫的那一支）的列舉從 v 0–5 擴到 v 0–11，條目 864 → 1,728（見 §6）。
  - 版本 v13.34／T628。
- `fp.json`：只搬 `blocks.fam`、`blocks.count`、`version`、`anchor`、`generatedAt` 五個欄位（在暫存複本跑 `--expect=bld,block559`），葉子指紋維持 Windows 基線。
- `smoke.js`：自檢清單加 `vkey628`。
- 新增 `probe628.js`：順序無關、影響規模、快取張數，並存修前修後樣張。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `vkey628`（煙霧常駐，5 項） | k 1–3 × lv 1–3 × 2×1 × v 0–5：**54/54 組 v 與 v+6 各拿自己的精靈** ✓；其中 53 組的圖真的不同、18 組連原型或立面款式都不同 ✓；**守衛有效：關閉閥門時 54/54 組共用同一張** ✓；閥門條目已清 ✓；耗時 271ms |
| `probe628.js` ① 順序無關（種子城住宅近景，兩種參觀順序） | 修正後**逐像素相同**（差 0 像素）；修正前**不同**（差 18,080 像素，右上一塊街區：先看住宅時是白藍半木造別墅＋米色聯排，先看港區時變成紅磚雙拼＋藍灰平頂樓）⇒ 守衛有效 |
| ② 影響規模（逛遍全城後逐塊查） | 種子城 191 塊住商工街區：修正前 **24 塊（62 格）畫成別的 v 的圖，24 塊全都看得出不同**（住宅 10、商業 6、工業 8）；修正後 **0 塊** |
| ③ 快取與耗時（Pages 8 個鏡頭） | 街區精靈 127 → 148 張（+17%）；8 張拍完 6.35 → 6.79 秒 |
| 修前修後（Pages 同鏡頭，差異像素） | 中景白天 16,338；近景白天 49,821；住宅近景白天 52,503；住宅近景夜晚 57,294；遠景 0（z=0.45 畫迷你方塊，不用街區精靈） |
| 煙霧（雲端容器） | **綠 3／紅 0**，28.0–29.6s，0 console error |
| `fp.js --check` | 葉子與 T626 相同：只有環境差 4 葉 |
| `fp.js --expect=bld,block559`（暫存複本） | block559 `d6ca3b88 → ff961a2b`（864 → 1,728 條）；**只取 v 0–5 那 864 條重算＝`d6ca3b88`，與 T624 基線逐條相同**，變動全部來自新增的 v 6–11；七軸棘輪 OK（151 族無一下降） |
| 樣張 | `shots628/T628_{home_day,near_day}_cmp.png`（洋紅格＝會變的地方）、`T628_order_{old,new}.png`（同一鏡頭、兩種參觀順序）；不進版本庫，已傳業主 |

## 6. 施工紀錄（如實，含失敗）

- **卡面估錯了規模。** 卡面寫「大約一半的街區可能畫成別的原型」，實測種子城是 **24/191 塊（12.6%）**。要畫錯，得同時有同 k、同 lv、同尺寸、v 差 6 的另一塊街區，而且它先被畫到；一半是上限，不是實況。
- **施工中追加：指紋覆蓋 v 6–11。** `blockFp536` 原本只量 v 0–5；T628 以前 v 6–11 的街區借用 v−6 的圖，所以它們自己的精靈**從來沒進過指紋**。修正後城裡約一半街區畫的是 v 6–11 的圖，不量就等於沒有守衛，所以一起擴。這一條不在卡面 §2，驗收（v 0–5 子集重算等於舊基線）是施工中補的。
- `index.html` 裡有兩支 `blockFp536`：IIFE 內 50474 行附近的舊版（11 種尺寸）已被後面的 `window.GV.blockFp536`（16 種尺寸，`fp.js` 用的這支）覆蓋，是死碼；只改了用得到的那支，舊的沒動。
- 修正後有些街區換成它們本來該有的原型，例如住宅近景中間一群紅頂小屋之間出現一棟藍色平頂的現代樓——這是原型表替那個 v 設計的樣子，以前只是剛好被別的 v 的圖蓋掉。這樣的混搭好不好看是設計問題，本卡沒動原型表；業主不喜歡可以用閥門退回，或另卡調原型表。
- **沒做成的：**
  1. 原型表的混搭（小屋群裡的現代平頂樓）沒有逐款檢查。
  2. 繪製迴圈用**起點格**的 lv／v 取圖，`rciBlockOrigin547` 卻算的是**最高等級那一格**的 lv／v（繪製沒用到）；兩者不一致，本卡沒動。
  3. 死碼的舊版 `blockFp536` 沒刪。

