# T655 — 樹照地形長

**輪次**：r102
**基線**：`c900042`（v13.59／T654）；卡面寫的時候是 `5c049c6`
**狀態**：已落地（v13.60）；卡面先單獨提交 `e6f93f1`
**業主決定**：2026-09-25 決策單第四批「樹要不要照地形長」選 **B 看地形**（`docs/DECISIONS.md`）。T654–T655 一次佔號，一張一張做。

---

## 1. 這一輪要解決什麼

T652 讓樹一片一片成林，但主樹種在 5×5 格裡隨機挑，不看地形：水邊、沙灘、高地、平地長的樹都一樣。

## 2. 改什麼／不改什麼

**改**：畫樹時先看這格的地形分類，再在那一類的樹種裡照 T652 成林（每 5×5 格一個主樹種、七成五照主樹種）：

| 地形 | 判斷 | 樹種 |
|---|---|---|
| 水邊 | 周圍 8 格有水 | 垂柳、鑽天楊 |
| 沙岸 | 周圍 8 格有沙（且不靠水） | 棕櫚、橄欖 |
| 高地 | `t.el===1` | 松、柏、白樺、雲杉 |
| 平地 | 其他 | 闊葉樹甲乙、灌木、銀杏、楓、櫻花、紫葉李、橡樹、懸鈴木、山毛櫸、樟樹、榕樹、鳳凰木 |

只改畫、不改存檔。T654 關閥門時各類去掉新樹種。逃生閥 `window.__noTerrainTree655`＝回到 T652（不看地形）。自檢 `terrainTree655`；版本 v13.60／T655。

**不改**：樹的位置；南北氣候帶（業主選 B，不分南北）；櫻花道（靠路的平地樹照舊染成櫻花）。

## 3. 怎麼算做完（動手前寫下）

1. **自檢 `terrainTree655`**（煙霧常駐）：分類函式——靠水→水邊、靠沙不靠水→沙岸、高地→高地、其他→平地；每一類挑出的樹種都在該類清單裡（各 50 個座標）；閥門可關。
2. **全圖**（`probe655.js`，種子城）：每棵樹的分類與畫出來的樹種對得上的比例新版 100%、關閥門明顯低；另存水邊、沙岸、高地近景。
3. **煙霧** 3 連跑綠；GitHub Actions 綠；`fp.js --check` 綠。

## 4. 施工

- `index.html`：
  - `TERRAIN_SP655`：四類樹種清單（1 起算，同 `t.tree`）。平地 1,2,4,5,6,8,10,11,16–20；水邊 7（垂柳）、15（鑽天楊）；沙岸 9（棕櫚）、21（橄欖）；高地 3（松）、12（白樺）、13（柏）、14（雲杉）。
  - `terrainClass655(x,y)`：看自己和周圍 8 格（3×3），有水→水邊；沒水有沙→沙岸；都沒有、自己 `el===1`→高地；其他→平地。
  - `terrainList655(c)`：T654 關閥門時濾掉索引 > 13 的新樹種（水邊剩垂柳、沙岸剩棕櫚）。
  - `groveTree652` 加第 4 個參數 `L`（這一格能長的樹種）：七成五照 5×5 主樹種（同一組雜湊），其餘把原本的 `t.tree` 對映進清單（`L[(v-1)%L.length]`）；關 T652 閥門時只做對映。不給 `L` 時跟 T652 一模一樣。
  - `terrainTree655(v,x,y)`：閥門 `__noTerrainTree655` 關＝`groveTree652(v,x,y)`（回到 T652）。畫樹那一行改呼叫它。
  - `drawPath652` 改成認 `groveTree652(...)` 或 `terrainTree655(...)`（成林改由地形分類呼叫）；新增 `drawPath655`。
  - 自檢 `terrainTreeSelftest655`；`GV` 開 `terrainClass655`、`terrainTree655`、`TERRAIN_SP655` 給守衛用；版本 v13.60／T655。
- `smoke.js`：自檢清單加 `terrainTree655`。
- 新增 `probe655.js`。
- `fp.json`／`style.json` 不動（精靈沒變）。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `terrainTree655`（煙霧常駐，6 項） | 分類 6 種擺法（全草地、斜角有水、旁邊有沙、沙水都有、高地、高地外圈兩格有水）都對 ✓；四類各 50 個座標挑出的都在清單裡 ✓；純函式、每類過半照主樹種 ✓；T654 關閥門各類不含新樹種、不會空 ✓；關閥門 50 個座標逐一等於 T652 ✓；畫樹時有呼叫 ✓ |
| `probe655.js` 全圖（種子城 426 棵樹） | 平地 249、水邊 34、沙岸 60、高地 83。分類與樹種對得上：新 **100%**（四類都是）／關閥門 **41.8%**（平地 60.6%、水邊 5.9%、沙岸 13.3%、高地 20.5%） |
| `probe655.js` 近景（z=2 正午，關紅綠燈） | 水邊 (67,4) 差異像素 38,328；沙岸 (55,31) 126,771；高地 (62,27) 230,456 |
| `fp.js --check` | 葉子零變動、超街區指紋不變、棘輪 OK |
| 煙霧（雲端） | **綠 3／紅 0**，28.5–28.7s；`grove652`、`tree654` 照過 |

樣張：`shots655/T655_{water,sand,high}_{new,old}.png`。

## 6. 施工紀錄（如實，含失敗）

- 一次就綠，沒有回滾。
- **分類順序是「水＞沙＞高地＞平地」**：高地邊上靠水的樹算水邊（長垂柳、鑽天楊），不算高地；卡面第 2 節表格就是這個順序，照做。
- **「周圍 8 格」實作成 3×3 含自己**：樹種在沙上（玩家種的）也算沙岸。
- **改了 T652 的 `drawPath652`**：畫樹那行不再直接呼叫 `groveTree652`，成林改在 `terrainTree655` 裡面叫；`grove652` 自檢的「畫樹時有呼叫」改成兩種寫法都認。
- **5×5 主樹種在四類裡各自挑**：同一塊 5×5 裡水邊一排、平地一片時，兩類各有各的主樹種，所以水邊一排不會跟後面平地同一種。
- **看得出來的問題**（近景看圖）：平地清單 13 種仍然很雜，一片平地林還是會有楓紅、紫葉、鳳凰木並排（來自 25% 非主樹種和相鄰 5×5 區塊的主樹種不同）。要再收要調 T652 的七成五或區塊大小，屬觀感，出圖給業主選，不在這張做。
- **沒做**：南北氣候帶（業主選 B 不分南北）；亞熱帶樹種在雪地照畫；落葉樹冬天不變禿。

