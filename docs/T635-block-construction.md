# T635 — 施工中的超街區蓋著一層金色線框：升起期的切片與鷹架每條分段各畫一次

**輪次**：r82
**基線**：`687f96d`（v13.39／T634）
**狀態**：✅ 綠燈（卡面先單獨提交 `7b2762a`，驗收條件寫在動手之前）

---

## 1. 這一輪要解決什麼

待辦「超街區施工階段」。進場探針：暫時補丁加一個設定工期的出口，把種子城住宅近景的 12 塊超街區設成 age 5／7（T259 的「逐層升起」期），拍照後還原。

- **畫面**：整片住宅區蓋著金色線框：細長的鷹架柱、橫桿、吊車臂交錯成網格，從住宅區延伸到摩天樓旁；施工中的樓體也比單棟施工時更不透明。
- **根因一：每條分段各畫一次。** 超街區拆成 w+h−1 條分段、每條各有深度（T555）。升起期（`constrRise`）的分支排在分段裁切之前，所以每一條都把**整張**升起切片畫一次、沒有裁切：深度錯（後面那條的切片蓋到前面的物件上）、半透明的淡入疊了好幾層。樓板線、鋼筋頭、吊車、T599 鷹架與工人、夜間趕工燈也每條各畫一次：12 塊街區，鷹架層畫了 **46 次**（剛好是各街區分條數的總和）。
- **根因二：鷹架照單棟算。** 位置與寬度用 `bd.sz`（超街區是 1）＝起點格那一格，高度卻用整張超街區精靈的高度，所以柱子細長、落在街區後角。
- 一般遊戲裡，玩家劃好分區後相鄰同類建築一起開工、馬上合成超街區，城市成長期會常常出現這個畫面。
- 另外看到的：打地基期（age 0–3）單棟有工地畫面（圍籬、開挖、建材），超街區被特意排除（`!o.block547`），直接畫成 78% 透明度的完工樓。這是「沒有」，不是「壞了」，本卡不做（見 §2 不改）。

## 2. 改什麼／不改什麼

**改**（只動升起期 age 4–8 的超街區）

1. 升起切片改在分段裁切裡畫：每條分段只畫自己那一條的切片，深度照分段走（與完工時的分段畫法同一個裁切）；夜光遮擋（T629）照同一個裁切記。
2. 樓板線、鋼筋頭、吊車、T599 鷹架與工人、夜間趕工燈：**每塊只畫一次**，在最前面那一條（`blockD1629`）畫，蓋在整塊樓體前面；位置用整塊地的中心，寬度用整塊地的寬度（`(w+h)×16z`，單棟 sz×sz 時與原式相同）。
3. 逃生閥 `window.__noBlockConstr635` 退回舊行為。
4. 自檢 `blockConstr635` 掛進煙霧。版本 v13.40／T635。

**不改**

- 單棟建築的施工畫法（逐像素不變）。
- 打地基期（age 0–3）超街區沒有工地畫面：要不要畫、畫多大，屬新增美術，另案。
- 施工階段看起點格的 age（整塊街區同一個施工階段）：規則不變。

## 3. 怎麼算做完（動手前寫下）

1. **每塊一次**（自檢）：種一塊 3×1 商業 lv2、age 5 的超街區，畫一幀：鷹架層（`__t599Constr`）計 1 次；**關閉閥門時計 3 次**（守衛有效）。
2. **切片有裁切**（自檢）：同一塊，升起切片的每一次繪製都在分段裁切裡（攔截 `clip` 與切片的 `drawImage`）；關閉閥門時 0 次在裁切裡。
3. **單棟不變**（自檢）：1×1 施工中建築，修正前後畫面逐像素相同。
4. **種子城**（`probe635.js`）：住宅近景 12 塊超街區設成 age 5，鷹架層 46 → 12 次；修前修後樣張給業主；完工狀態（age ≥ 9）修前修後 0 像素差。
5. **煙霧**：雲端 3 連跑綠；GitHub Actions 綠；Pages 自動部署。
6. **指紋**：`fp.js --check` 全綠（精靈沒變）。

## 4. 施工

- `index.html`
  - 升起期（`constrRise`）：超街區分段（`strip635`）先套 `clipBlockDiag555` 再畫切片，夜光遮擋（T629）照同一個裁切記；計數 `__t635Clip`。
  - T259 樓板線、鋼筋頭、吊車、夜間趕工燈與 T599 鷹架、工人：超街區只在最前面那一條（`blockD1629`）畫一次；**位置與尺寸照原式**（起點格、單棟尺寸），見 §6。
  - **施工中追加**：T168 鷹架網（立桿、橫板、斜撐、平台、工人、安全網，照精靈整個外框畫）：超街區只在最前面那一條畫一次，並用 T606 的 `ovClipBegin606`／`ovClipEnd606` 剪進建築剪影；計數 `__t635Net`、`__t635Sil`。
  - 單棟建築走的程式路徑不變（`G=ctx`、`X0=bx`、`Y0=by`，幾何原式）。
  - 逃生閥 `window.__noBlockConstr635` 一次退回以上全部舊行為。
  - 新增自檢 `blockConstrSelftest635`（6 項）；測試出口 `GV.testAge635(x,y,w,h,age)`（設定一塊地的工期，回傳原值，給探針用）。
  - 版本 v13.40／T635。
- `smoke.js`：自檢清單加 `blockConstr635`。
- 新增 `probe635.js`。

## 5. 守衛（可斷言的事實）

| 項目 | 結果 |
|---|---|
| `blockConstr635`（煙霧常駐，6 項） | 種出 3×1 超街區 ✓；施工疊層每塊一次（街區＋單棟＝2）✓；切片 3 條都在裁切裡畫 ✓；T168 鷹架網每塊一次、街區那張剪進剪影 ✓；**單棟施工那一塊畫面逐像素不變**（192×290）✓；**守衛有效：關閉閥門時疊層 4 次、鷹架網 4 次（街區每條一次＋單棟）、裁切 0** ✓ |
| `probe635.js` 種子城住宅近景 | 15 塊超街區（52 條分段）設成 age 5：施工疊層 **52 → 15 次**；T168 鷹架網 **52 → 15 次**（15 張全剪進剪影）；切片在裁切裡畫 0 → 52 次；施工中修前修後差 259,310 像素；**完工狀態修前修後 0 像素** |
| 煙霧（雲端容器） | **綠 3／紅 0**，28.9–29.2s，0 console error |
| `fp.js --check` | **全綠**（精靈沒變） |
| 樣張 | `shots635/T635_options2.png`（修正前／A／B／C 四格；不進版本庫，已傳業主） |

## 6. 施工紀錄（如實，含失敗）

- **卡面低估了範圍。** 卡面只寫到 T259／T599 的疊層。第一版修完（切片裁切、疊層每塊一次、照整塊地擺）實拍仍是一片金色格線；另做「超街區不畫鷹架吊車」一檔，格線也還在，才追到真正的大宗：T168 鷹架網照**精靈整個外框**畫（超街區精靈的外框又寬又高、大半透明），而且同樣每條分段各畫一次。施工中追加：每塊一次、剪進建築剪影。
- **卡面 §2 第 2 條「照整塊地擺」沒有照做。** 修掉 T168 之後拍了三檔：A 位置尺寸照原式（只去掉重複）、B 照整塊地放大、C 超街區不畫吊車與 T599 鷹架。B、C 是觀感選擇（常駐規則 4），不替業主挑：提交版用 A（只修錯），B、C 補丁留在暫存區，對照圖傳業主（決定 D8）。
- 探針的街區數與進場探針不同：進場挑 w×h≥3 的 12 塊（46 條分段），`probe635.js` 挑 w×h≥2 的 15 塊（52 條）。
- 出檔腳本第一次在一次 `evaluate` 回傳 4 張 PNG（約 3.3MB），CDP 卡住 8 分多鐘被逾時中斷；改成一次一張後 25 秒跑完。
- **沒做成的：**
  1. 打地基期（age 0–3）超街區沒有工地畫面（直接畫成 78% 透明的完工樓），另案。
  2. 施工階段看起點格的 age：同一塊街區裡新舊格子混在一起時，整塊跟著起點格走，規則沒動。
  3. T168 鷹架網剪進的是「完工後」的剪影，所以樓體還沒蓋到的上半部也看得到半透明的網（單棟本來也是這樣畫）。
  4. B／C 兩檔等業主挑。
